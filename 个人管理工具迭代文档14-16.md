# 迭代14

- 重构 `Desktop` 下的文件组织方式，建立清晰分层（不只 Page / Control）。
  - **目录**
    - `Desktop/Page/`：完整功能页模块。主窗口 **五大页签**（日程记录、收藏夹、体重、界面、设置）均拆成独立 Page 模块。Page 可依赖任意数量的 Control，以及下方的非 UI 模块；Page 之间尽量不互相直接依赖实现细节。
    - `Desktop/Control/ControlName/`：控件实现。Control **不能依赖 Page**。
    - `Desktop/Services/`（或等价名，如 `Infra/`）：既不是 Page 也不是 Control 的基础设施。**需要拆出**，至少包括：Noco 通信与 schema（现 `NocoClient` / `SchemaService` / `NocoRecordStore` 等）、本地存储与迁移、会话/`AppSession`、路径与用户账户、主题与窗口几何、Docker 拉起等。原则：无 WPF 页面生命周期；可被 Page / Window / 启动流程共用；禁止反向依赖 Page/Control。
    - 根目录或 `Desktop/App/`：仅保留启动壳（`App`、托盘、登录编排入口等），避免继续平铺所有 `.cs`。（使用App子目录来存放启动壳）
  - **是否还要拆「其它模块」**  
  **要。** 仅拆 Page/Control 不够：Noco、本地库、迁移、主题等会继续污染 UI 层。本轮以「能独立引用、单向依赖」为目标拆 Services，不必做成多项目/多程序集（单项目子目录即可）；若某块日后要给手机端复用，再考虑抽类库。
  - **Page vs Control 职责**
    - **业务真相与跨控件共享状态、与存储/Noco 的读写、跨窗口协作的计时与调度**：由 **Page**（或持有 session 的壳）统一负责。
    - **控件可以保留临时 UI 状态**（如拖动中的选区、预览缩放、输入框未提交内容、悬停高亮、短生命周期动画）。控件 **不直接写库 / 不改业务 session 字段**；提交时把表单结果回传上游，由 Page 落盘或调 Services。
    - 控件原则上不承担「业务向」长计时与持久数据源；纯 UI 用的短计时器（如拖拽吸附预览）允许留在控件内。
  - **单独调试**  
  **不作为普遍准则。** 仅当某个控件需要专项验证时，再为该控件在 `Probes`（或临时宿主）里加测试入口；多数控件随所属 Page / Window 回归即可。
  - **Window 定位（本轮约定）**
    - **主窗口**：壳；只负责页签切换、托盘/全局快捷键、挂载五个 Page，不承载各页业务细节。
    - **功能子窗**（新建/编辑任务、奖励愿望、收藏编辑、任务执行、提醒、二级密码、登录、缩略图选区、块样式编辑等）：**不是 Page**，也不塞进 Control。归入 `Desktop/Window/`（或按域分 `Window/Task/` 等）。Window 可依赖 Control 与 Services；**不可依赖 Page**。需要业务数据时由打开方传入 `AppSession` / 回调 / 结果对象，避免 Window ↔ Page 双向引用。
    - 已偏「可复用面板」的 UI（如 HLS 取色、任务卡）进 Control；整窗流程（对话框、独立工具窗）留在 Window。
  - **本轮落地范围（执行约束）**
    - 成功标准：`App` / `Services` / `Window` / 五大 `Page` 目录分层到位；单向依赖（Control/Window 不依赖 Page；Services 不依赖 Page/Control）。
    - **主窗口壳可持有** `AppSession` 与进程级运行态（进行中任务、托盘退出弃跑、F5 全量刷新转发等）；「不承载各页业务细节」不等于壳零状态。
    - **Services 允许依赖 WPF 类型**（如 `Theme` 的 Brush）；禁止的是依赖 Page/Control，而非「零 UI 程序集」。
    - **日程 Page 本轮整页迁出即可**，不强制大量拆 Control；其它页同理，Control 按痛点少而精抽取。
    - 现有 Window 内部直接写库可暂留为债务，不阻塞本轮目录与 Page 拆分；新抽 Control 须遵守回传上游落盘。
    - 建议切片：① Services/Window/App 归位 → ② 五 Page + 主窗变壳 → ③ 按需抽 Control。

# 迭代15

- 双击日程记录块时触发与双击任务卡片一样的功能，打开任务编辑界面
- 取消编辑任务窗口中「日程记录块颜色」「日程记录块样式」「样式颜色」标签和同行的元素，仅保留样式预览和高级编辑按钮。
- 「缩略图」文字右边分为两行，一行是缩略图和「浏览」按钮，一行是「取消缩略图」按钮和「重新截取」按钮。缩略图固定高度 64px。
- 取消「截止时间」和「提醒时间」这两行，暂时不删除对应的功能和数据。删除「截止型」「灵活型」任务类型。（旧数据若仍含这两类：本轮无视，等报错再处理。）
- 为任务编辑界面添加「直接生产力任务」勾选选项。悬浮提示：「勾选：当前任务为我们希望提高每日有效执行时间的任务」
- 样式编辑窗：将「X/Y 重复偏移」改为「X/Y 偏移」，新增「X/Y 累积偏移」。累积偏移为 t 时，第 n 行偏移 n·t px（行间错开）。旧 Offset 语义归入「偏移」；累积默认 0。
- 切换样式层时数字参数显示旧值、滑块正确：属代码预期 bug，直接修。
- 使数字参数可以手动输入。
- 记录日程：弹窗可选 10 分钟 / 20 分钟 / 30 分钟 / 1 小时 / 至今。从上次任务结束起记对应时长；「至今」= 原行为；若结束时刻会超过现在则该按钮灰掉。

# 迭代16

- 修改日程记录块样式预设的保存格式，使其可保存为单个 json 文件，便于复制分享。存储目录可含子文件夹；下拉中表现为可收起分组（组标题=文件夹名，右侧三角展开/收起）。
- 若当前样式层不支持特定参数，将该参数灰掉（滑块不可移、不可输入）。需先定 Kind→参数支持矩阵。
- （承接 15 未决）样式预设编辑窗口其它后续项
- 新增“笔记钉”功能。
  在日程记录中，鼠标处于右侧日程记录界面时，会显示横向虚线标记当前鼠标位置，并且在最左侧时间标签处注明当前鼠标所处的时间点。此时用户点击右键可添加笔记钉。笔记钉以日期-时间+点击时鼠标所处的列的左到右百分比的形式记录自身的坐标。新增笔记钉时，打开笔记钉编辑窗口。用户可以输入文本笔记。保存后，鼠标左键点击笔记钉可以展开笔记钉笔记内容，点击别处关闭内容显示。双击笔记钉可重新进入编辑界面。
- 在日程记录块样式编辑界面新增样式图层：纯色
  - 为所有样式图层新增选项：混合模式。当前包含选项：普通、正片叠底、叠加、柔光
  - 这些模式不即时运算，而是在保存之后，计算各图层最终应以普通模式绘制的颜色，然后全部以普通模式绘制，避免不必要的性能开销
    - 需要验证避免这些性能开销的合理性。真的有那么大性能开销吗？


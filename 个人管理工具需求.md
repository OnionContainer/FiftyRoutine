个人管理系统

要求：连接本地部署的nocodb，在其中存储数据。本地nocodb在docker中，可以以localhost的形式提供服务，我们的程序用api与其互动。

功能：

任务管理

1. 任务列表。用户可以通过gui新建任务并插入任务列表。任务按执行周期分为以下类型：每日重复型（要求每日完成）、截止型（在特定时间前完成）、灵活型（随便什么时候干都行）、灵活重复型（可以反复执行）任务拥有如下属性

- 登记日期
- 完成日期（每日重复何灵活重复的任务可以记录多个完成日期，而且不会被标记为已完成）
- 奖励等级（1-5整数）
- 提醒时间（在指定的时间用windows消息功能提示用户应执行此任务）

1. 奖励池。完成奖励等级1-3的任务后，可以获得对应次数的抽取机会，从奖励池中抽取奖品。其中有一些奖品为愿望单额度。
2. 愿望单。将想要但是又有点犹豫的东西记在愿望单中。如果满足了愿望单额度，就可以从愿望单中选择物品进行购买，并且扣除对应的愿望单额度。
3. 执行记录。用户可以点击一个任务来开启执行记录。再次点击时记录任务开始、结束的时间。
4. 执行历史。用户可以打开执行历史，用周日程表的形式观看指定周的执行历史。
5. 每日重复型任务固定为L1级奖励（即获取一次抽取机会）。在成功执行一周之后，将被标为巩固期，有1/3的概率获得L1级奖励。在成功执行两周之后，将被标为观察期，有1/10的概率获得L1级奖励。在任何时期都有1/100的概率在每日重复型任务中额外获得一次L3奖励。

收藏夹

1. 可以收藏图片，并附带来源。可以给收藏的图片打上tag。
2. 在程序内内置的查看器中查看图片缩略图集。双击可以用honeyview打开图片。另外提供批量选择、从数据库中导出的功能。还可以直接将图片复制到剪贴板中。
3. 在查看器中勾选私密tag时需要输入二级密码。
4. 还可以收藏文字（链接）和视频。可以为它们添加缩略图。
5. （未来功能）由AI控制cli 浏览器，自动将X上的点赞内容存为收藏图片。
6. 查看器中可以选择“untagged”来查看未分配tag的图片。



## 迭代 1

窗口名称按 `窗口信息.md` 的文档用名来写。任务执行窗已写入该清单。

### 任务执行循环

在 **主窗口 · 任务页**：双击一张任务卡片，或先选中再点「开始执行」，弹出 **任务执行窗**。

**任务执行窗**（新窗，全局同时只开一个，表示该任务正在执行）：

- 右上角关闭键：与「退出」相同（成功结束本次执行，见下）。
- 倒计时进度条：默认 30 分钟。满 30 分钟 **不自动中断**，只发提示（弹窗 + 声音）。该提示里可以直接退出本次执行。
- 「退出」：用户确认本次执行成功结束。程序写后台记录，并按规则发奖励。
- 「失败」：用户宣布本次没做完。关闭任务执行窗。本次仍写入记录，但标记为失败，不发奖励。
- 「暂停」：去做别的事。暂停期间倒计时停止，这段墙钟时间不计入任务执行时长。

奖励结算：L1 / L2 / L3 以 **30 分钟有效执行** 为单位。有效执行满 30 分钟，且用户点「退出」（成功）后，才结算该等级奖励。未满 30 分钟的成功退出：记记录、不发奖。

用户可以设 **L0** 任务：只做快速记录，无奖励（例如吃饭）。

产品文案是否把「任务」改成「待办事项」等，尚未拍板，见提问。

任务也要有缩略图，并考虑像收藏夹那样拖入图片来创建。任务与收藏可能共用一部分代码/数据结构，两个页签区别显示。本轮做到哪一步见提问。

### 日志（主窗口 · 本周执行页）

更新 **主窗口 · 本周执行页** 的样式（原文「日志窗口」；现有产品里对应这个页签，不是独立窗口）：

- 本周 7 天分 7 列；上方一行标明该列日期。
- 左侧一列时间参考线：每天 8 条，每 4 小时一条，从 0 时起（0、4、8、12、16、20、24）。
- 每个 **执行完成** 的时段，在对应日期列画一个长方形：顶 = 开始时间，底 = 结束时间。

在 **新建任务窗** 里可设任务「颜色」（点颜色弹出系统取色板）。该颜色作为 **主窗口 · 任务页** 卡片底色，以及本周执行页色块底色。

**主窗口 · 任务页** 不再用表格，改为卡片矩阵，默认每行三张。卡片上显示任务图标和信息。

#### 当前日志数据怎么存（已查代码）

NocoDB 表 `sessions`：字段 `StartedAt`、`EndedAt`，用关系列挂到 `tasks`。开始执行时插入一行并写 `StartedAt`，`EndedAt` 为空表示进行中；结束时补上 `EndedAt`。

**主窗口 · 本周执行页** 现在只是把这些会话打成文字列表，没有按小时画块。

还没有：失败标记、暂停分段、任务颜色、任务缩略图、L0。完成打卡仍在子表 `completions`（`CompletedOn`），和执行时段是两套数据。奖励目前主要在点「完成」时结算，不是在执行满 30 分钟时。

---



## 提问

能从语境对上的名称已在上文替换。下面是对不齐、或不够落地的点。每条附默认方案；不回复则按默认做。

1. **30 分钟到点的提示弹窗**是新窗，还是复用 **任务提醒窗**？
  现有任务提醒窗只有文案 +「知道了」，不能从那里退出执行。  
   **默认：** 复用任务提醒窗，到点文案改为「已满 30 分钟」，按钮「知道了」和「退出」（退出 = 成功结束，与任务执行窗「退出」相同）。声音先用短促提示音（可先 `Console.Beep`，有空再接到 playground 那套合成音）。
2. **已有任务执行窗时又点「开始执行」/双击另一张卡？**
  **默认：** 不允许第二路。聚焦已打开的任务执行窗；状态栏提示「已有任务在执行」。
3. **暂停在本周执行页上怎么画？** 需求既要「顶=开始、底=结束」一整块，又要把暂停从执行时长里抠掉。
  **默认：** 色块按墙钟从首次开始画到最终结束（中间暂停仍占高度）；奖励和「满 30 分钟」只按有效执行时长（扣除暂停）。暂停不另存成多段；`sessions` 增加 `PausedSeconds`（或等价字段）。  
  按默认执行的基础上，在色块中添加较小的灰色色块表示此时在暂停。
4. **失败**怎么进库？
  **默认：** `sessions` 增加 `Outcome`：`success` / `failed`（进行中为空）。失败行仍有开始/结束时间，本周执行页照样画块，但用更淡或斜纹，避免和成功块混。
5. **L4 / L5** 还发不发奖？原文奖励池是完成 L1–L3 才抽。本轮只写了 L0 无奖、L1–L3 按 30 分钟结算。
  **默认：** L4/L5 本轮也记执行、不发抽奖次数（与现有「只有 L1–L3 进奖池」一致）。
6. **一次执行超过 30 分钟**发几次？例如有效时长 90 分钟再点退出。
  **默认：** `floor(有效分钟 / 30)` 次该等级抽奖机会。未满的余数不计。每日重复的巩固期/观察期概率仍套在「每一次 30 分钟结算」上。
7. **「完成」按钮和每日连续天数**还要不要？现在点「完成」写 `completions` 并立刻抽奖。本轮改成执行窗里结算。
  **默认：** 任务页去掉「完成」。成功「退出」且有效时长 ≥ 30 分钟时：写 `sessions`（success）+ 当天一条 `completions`（用来算巩固/观察）+ 发奖。不足 30 分钟的成功退出只写 session，不写 completion、不发奖。失败只写 session。
8. **「任务」是否改名「待办事项」？**
  **默认：** 本轮 UI 仍叫「任务」，包括页签名、新建任务窗标题。
9. **任务缩略图 / 拖入创建 / 与收藏共用**，本轮范围？
  **默认：** 本轮做：`tasks` 增加 `Thumb`（附件）和 `Color`；任务页卡片显示缩略图（无图用纯色块）；新建任务窗可选缩略图；任务页支持拖入/Ctrl+V 图片并弹出新建任务窗（预填缩略图，类似收藏编辑窗）。任务和收藏仍分表，不合并。双击卡片是进入任务执行窗，不是编辑；编辑用卡片上的入口或以后再做。
10. **颜色和缩略图在哪改已有任务？** 现在没有任务编辑窗。
  **默认：** 本轮只在新建任务窗里设颜色/缩略图；已有任务暂不提供编辑（避免和「双击=开始执行」抢手势）。
11. **本周执行页能否翻周？** 总需求写过「指定周」，页签名叫「本周」。
  **默认：** 本轮只画当前自然周（周一到周日），不提供翻周。
12. **任务执行窗要不要一直置顶？** 关掉主窗口会藏托盘，执行窗怎么办？
  **默认：** 任务执行窗不置顶、不随主窗口隐藏；关主窗口进托盘时执行窗仍留着。从托盘打开主窗口不影响正在执行的那一条。真正「退出」程序才结束执行：若仍有进行中的 session，按失败收尾（写 EndedAt + Outcome=failed）。



## 迭代 2

窗口名称仍按 `窗口信息.md`。本轮只定方案；落地实现时再改窗口清单（**主窗口** 加「设置」页签；奖励/愿望改卡片；任务双击改为编辑；新增 **奖励编辑窗**）。

### 目标

新增 **主窗口 · 设置页**，用来改界面样式参数（颜色为主，加卡片高度、开始任务图标等）。参数可以另存为命名副本。程序自带两套不能删的预设：**普通模式**、**黑夜模式**，设置页里切换即生效。

同一轮把列表改成统一的卡片交互：

- **奖励页、愿望单页**也改成卡片（与任务、收藏同一套卡片壳）。
- **双击卡片 = 编辑当前项**（任务、奖励、愿望、收藏一致）。迭代 1 里「双击任务卡 = 开始执行」作废。
- 卡片 **固定高度**（像素），高度是主题参数；不再用 `UniformGrid` 那种按剩余空间均分的占比高度。
- 任务卡缩略图 **右上角叠一层「开始任务」按钮**：显示图片、不显示文字，四周圆角。初始图为白底绿色播放键；换哪张图在主题设置里调。

任务自己的「颜色」（新建任务时取的色，画在卡片和本周执行块上）是任务数据，不是主题参数，见下「不要纳入主题的」。

### 现状（已查代码）

技术栈：WPF（`net8.0-windows`），窗口有的用 XAML，有的在 C# 里拼（启动准备窗、二级密码窗、任务提醒窗）。没有第三方 UI 库。

`App.xaml` 里几乎没有主题：只给所有按钮设了 Padding / Margin。窗口客户区、文字、边框都走 WPF 系统默认（浅色）。

颜色现在散落在三处：

1. **XAML 写死**：收藏编辑窗预览底 `#F4F4F4`、边 `#DDD`；新建任务窗色块边 `#CCC`。
2. **代码写死**：任务提醒窗背景 `WhiteSmoke`；本周执行列底 `(250,250,250)`、参考线 `Gainsboro`、失败框 `Gray` 虚线、暂停条灰 `(160,160,160)`；任务卡选中边 `SteelBlue`；收藏卡边 `LightGray`。提示字靠 `Opacity` 0.55–0.7 变淡，没有独立的「次要文字色」。
3. **任务数据**：`tasks.Color`，默认 `#8EB4E3`。卡片底和本周色块用这个，跟窗口背景不是一回事。

`DataGrid` / `TabControl` / `ToolBar` / `StatusBar` / `ListBox` 没有自定义模板，暗色下会很难看，除非给这些控件补 Style。**奖励现在是** `ListBox` **纯文字，愿望单是** `DataGrid`**。** 改成卡片后这两处不再依赖表格/列表的暗色模板，主题主要打在卡片壳上。系统对话框（MessageBox、取色板、文件选择器）和托盘图标不受 WPF 资源管。

任务页卡片墙用的是 `UniformGrid Columns=3`：每一行平分宽度，**行高也平分剩下的垂直空间**，所以卡片会随窗口变高变矮（占比高度）。收藏夹已经是 `WrapPanel` + 固定缩略图尺寸（140×140），更接近本轮要的固定高度。任务卡缩略图高 88，没有叠按钮；双击直接进任务执行窗。奖励、愿望没有卡片，也没有编辑窗（愿望只有「添加」用的 **愿望编辑窗**；奖池行目前不能改）。

本仓库 `playground` 已经用本地 JSON 存参数，说明「命名副本落盘」这条路是熟的；个人管理这边还没有类似文件。初始「开始任务」图标已用 Python 生成：`Personal_Management/Desktop/Assets/start-task.png`（128×128，圆角白底、绿色播放三角，圆角外透明，方便叠在缩略图上）。

### 可行性（当前技术栈）

**可行，不必换框架、不必加 UI 库。** WPF 本来就为换肤设计：

- 把颜色做成带名字的 `SolidColorBrush`，放进 `Application.Resources`。
- XAML 用 `DynamicResource`（不要 `StaticResource`），运行时改资源即可整窗刷新。
- 代码拼出来的界面（任务卡片、本周画布、提醒窗、二级密码窗、启动准备窗）读同一套资源，或订一个「主题变了」的通知再重画。本周执行和卡片墙本来就是代码画的，换主题时重跑现有的 `RenderWeekBoard` / `RenderTaskCards` / `RenderFavGrid`，并同样重画奖励卡、愿望卡。
- 预设 = 两份写死的参数表（普通 / 黑夜）；用户副本 = JSON 文件。设置页改滑块/取色/图标路径，写回当前副本并立刻 `Apply`。
- **固定卡片高度：** 任务/奖励/愿望/收藏都改用 `WrapPanel`（或等价流式面板）+ 卡片 `Height = card-height`，不要再用会均分行高的 `UniformGrid`。WPF 里给 `Border.Height` 写死像素即可，主题一改就重画。
- **开始任务按钮：** `Border`/`Button` 设 `CornerRadius`，内容是 `Image`（源为主题里的文件路径）。叠在缩略图右上角用 `Grid` + `HorizontalAlignment=Right; VerticalAlignment=Top` 和一点 Margin。点按钮 `Handled` 掉，避免冒泡成选中/双击编辑。图片解码失败时回退到包内默认 `start-task.png`。
- **双击编辑：** 收藏已经是双击进 **收藏编辑窗**。任务复用 **新建任务窗** 做成编辑模式（或同一套窗改标题「编辑任务」）；愿望复用 **愿望编辑窗** 预填；奖励目前没有编辑窗，落地时加 **奖励编辑窗**（名称 / 类型 / 额度 / 权重），并写入 `窗口信息.md`。

做得到的上限：所有我们自己画的客户区（主窗口各页签、各业务窗）。

做不到或不必强求：


| 表面                         | 原因                               |
| -------------------------- | -------------------------------- |
| 窗口标题栏、边框                   | 系统 DWM，除非以后上 `WindowChrome`      |
| MessageBox / 文件选择器 / 系统取色板 | Win32 / WinForms，跟 WPF 资源无关      |
| 托盘图标                       | 现在用 `SystemIcons.Application`    |
| 任务色块                       | 每条任务自己的 `Color`，换黑夜模式也不该改用户选的任务色 |


暗色下页签、工具条要用一组控件 Style（Background / Foreground）。愿望单不再用 `DataGrid` 之后，表格暗色模板可以不做。不要为这一轮引入 MahApps、MaterialDesign 之类。

启动准备窗出现得比主窗口早：启动时先读本地当前主题，再给启动准备窗上色，避免先闪一套系统白。

### 哪些颜色应统一成参数

原则：一个参数管一类表面，不要「每个页签一个背景」。任务色、暂停语义色分开，避免和窗口背景绑死。

#### 颜色（设置页可改）


| 参数名                      | 管什么                                                      | 现在大约是                         |
| ------------------------ | -------------------------------------------------------- | ----------------------------- |
| `window-background`      | 所有窗口客户区、主窗口各页签底（落地后含设置）                                  | 系统默认白；提醒窗 `WhiteSmoke`        |
| `surface-background`     | 卡片、工具条区域、底栏、输入区背后的「第二层」                                  | 基本等于窗口底；收藏预览 `#F4F4F4`        |
| `text-primary`           | 标题、正文、列表、表格字、倒计时                                         | 系统默认黑                         |
| `text-secondary`         | 底栏状态、拖入提示、「满 30 分钟不会自动停下」、空列表提示、预览占位字                    | 现在靠降低 Opacity                 |
| `accent`                 | 选中任务卡边框、进度条填充、默认按钮强调、页签选中暗示                              | `SteelBlue`；进度条系统蓝            |
| `border-subtle`          | 卡片边、预览框、取色条边、分割                                          | `#CCC` / `#DDD` / `LightGray` |
| `grid-line`              | 本周执行每 4 小时横线                                             | `Gainsboro`                   |
| `week-column-background` | 本周七列画布底（可与 `surface-background` 同值，单独留是因为日历格往往要比页面略灰/略深） | `(250,250,250)`               |
| `pause-overlay`          | 本周执行里表示暂停的小灰块                                            | `(160,160,160)` + 透明度         |
| `failed-overlay`         | 失败时段：降低不透明度 + 虚线用色                                       | 填充 Opacity 0.35，描边 `Gray`     |
| `control-background`     | TextBox / ComboBox / PasswordBox 底                       | 系统默认                          |
| `danger`                 | 「失败」按钮字或边（普通按钮仍走默认）                                      | 未单独设                          |


`window-background` 就是需求里说的那种：一处改，主窗口各页、新建任务窗、任务执行窗、愿望编辑窗、收藏编辑窗、奖励编辑窗、二级密码窗、任务提醒窗、启动准备窗一起变。不要再给「任务页背景」「奖励页背景」各开一个参数。

`surface-background` 给「叠在窗口底上的块」（卡片、预览框、底栏）。普通模式可以和 `window-background` 很接近；黑夜模式常用略亮一块的表面，避免整页死黑。

本周执行的任务长方形 **填充仍用任务** `Color`，只受 `failed-overlay` 调制。不要用主题色去覆盖用户选的任务色。

#### 非颜色（一并进副本，设置页可改）


| 参数名                  | 管什么                                            | 现在              |
| -------------------- | ---------------------------------------------- | --------------- |
| `font-size-body`     | 正文、表、卡片信息                                      | 系统默认 ~12        |
| `font-size-title`    | 任务执行窗任务名、抽奖结果                                  | 16–20           |
| `hint-opacity`       | 次要文字透明度（若已用 `text-secondary` 则可只留一个；落地二选一，见提问） | 0.55–0.7        |
| `card-corner-radius` | 任务/奖励/愿望/收藏卡圆角                                 | 8 / 6           |
| `card-columns`       | 四类卡片墙每行几张                                      | 任务页 3；奖励/愿望还不是卡 |


`card-columns` 对任务、奖励、愿望、收藏四墙共用（都是「每行几张」）。两个预设都写成 3。

#### 卡片高度与开始任务按钮


| 参数名                               | 管什么                                | 现在 / 预设                                             |
| --------------------------------- | ---------------------------------- | --------------------------------------------------- |
| `card-height`                     | 任务/奖励/愿望/收藏 **整张卡固定高度**（像素）        | 无；任务卡被 `UniformGrid` 拉高。预设约 200                     |
| `card-thumb-height`               | 卡顶图区高度（任务缩略图/纯色块、收藏图、奖励/愿望可用色块或占位） | 任务 88、收藏 140。预设约 96                                 |
| `start-task-icon`                 | 「开始任务」按钮用的图片路径                     | `Personal_Management/Desktop/Assets/start-task.png` |
| `start-task-button-size`          | 该按钮宽高（正方形，像素）                      | 无。预设约 28                                            |
| `start-task-button-corner-radius` | 该按钮四角圆角                            | 无。预设约 6；与图标自身圆角叠加                                   |


`card-height` 必须 ≥ `card-thumb-height` + 文字区。设置页改高度后四类卡片一起变，不要「任务卡高度」「愿望卡高度」各开一参数。

**开始任务按钮只出现在任务卡上。** 奖励/愿望/收藏没有这个按钮。工具栏「开始执行」可保留作「先选中再点」的备份，见提问。

图标在设置页用「浏览…」换一张 PNG；路径写进当前主题副本。普通模式和黑夜模式默认都指向同一张初始图（播放键在白底上，叠在深色缩略图上仍然能认出来）。用户若给黑夜模式另选深色底图标，存的是那份副本的路径。

#### 初始图标（已生成）

路径：`Personal_Management/Desktop/Assets/start-task.png`

128×128 PNG：圆角矩形白底、绿色播放三角（约 `#22A848`），圆角外透明。用标准库生成，不依赖 Pillow。实现轮若要重出，可跑：

```python
import struct, zlib
from pathlib import Path

W = H = 128
R, PAD = 26, 6
WHITE, GREEN, CLEAR = (255, 255, 255, 255), (34, 168, 72, 255), (0, 0, 0, 0)

def in_round_rect(x, y, x0, y0, x1, y1, r):
    if x0 + r <= x <= x1 - r and y0 <= y <= y1: return True
    if y0 + r <= y <= y1 - r and x0 <= x <= x1: return True
    for cx, cy in ((x0+r, y0+r), (x1-r, y0+r), (x0+r, y1-r), (x1-r, y1-r)):
        if (x-cx)**2 + (y-cy)**2 <= r*r: return True
    return False

def in_triangle(x, y, a, b, c):
    def cross(p, q, r):
        return (q[0]-p[0])*(r[1]-p[1]) - (q[1]-p[1])*(r[0]-p[0])
    s1, s2, s3 = cross(a,b,(x,y)), cross(b,c,(x,y)), cross(c,a,(x,y))
    return (s1>=0 and s2>=0 and s3>=0) or (s1<=0 and s2<=0 and s3<=0)

x0, y0, x1, y1 = PAD, PAD, W-1-PAD, H-1-PAD
play = ((46, 36), (46, 92), (96, 64))
rows = []
for y in range(H):
    raw = bytearray([0])
    for x in range(W):
        pix = CLEAR
        if in_round_rect(x+0.5, y+0.5, x0, y0, x1, y1, R):
            pix = GREEN if in_triangle(x+0.5, y+0.5, *play) else WHITE
        raw.extend(pix)
    rows.append(bytes(raw))

def chunk(tag, data):
    crc = zlib.crc32(tag + data) & 0xFFFFFFFF
    return struct.pack(">I", len(data)) + tag + data + struct.pack(">I", crc)

ihdr = struct.pack(">IIBBBBB", W, H, 8, 6, 0, 0, 0)
png = b"\x89PNG\r\n\x1a\n" + chunk(b"IHDR", ihdr) + chunk(b"IDAT", zlib.compress(b"".join(rows), 9)) + chunk(b"IEND", b"")
Path("Personal_Management/Desktop/Assets/start-task.png").write_bytes(png)
```

csproj 落地时把该 PNG 标成 `Resource` 或随输出目录复制，避免换机只有 JSON 路径、没有文件。

暂不开放：整窗宽高、本周 `pxPerHour`、按钮 Padding（已在 `App.xaml` 全局设置）。副本文件预留扩展位即可。

### 卡片内容与交互

四类卡片共用：`surface-background`、`border-subtle`、选中时 `accent` 边、`card-height`、`card-thumb-height`、`card-corner-radius`、`card-columns`。单击选中，**双击编辑**。


| 页   | 卡上显示                                             | 双击                                                  | 其它                              |
| --- | ------------------------------------------------ | --------------------------------------------------- | ------------------------------- |
| 任务  | 缩略图或任务色块；名称、类型、等级、阶段/状态；图右上角开始任务图标按钮             | 编辑任务（**新建任务窗**改标题「编辑任务」，预填并可改色/缩略图）。覆盖迭代 1「双击=开始执行」 | 点播放键 = 打开 **任务执行窗**（规则仍是全局只开一个） |
| 奖励  | 名称、类型（实物/额度/再抽）、权重、额度数字；顶区可用 `accent` 浅底占位（奖池无图） | **奖励编辑窗**（新窗）                                       | 「抽一次」仍在工具栏，抽的是奖池而不是某一张卡         |
| 愿望  | 名称、所需额度；顶区占位即可                                   | **愿望编辑窗** 预填后保存（Patch 原行）                           | 「兑换所选」仍针对当前选中卡                  |
| 收藏  | 已有缩略图墙，改为遵守 `card-height` / `card-thumb-height`  | 已是 **收藏编辑窗**，保持                                     | 勾选框保留                           |


单击开始任务按钮不得触发双击编辑。双击的计时仍走 WPF `ClickCount == 2`，按钮 `Click` 里 `e.Handled = true`。

### 不要纳入主题的

- **任务** `Color` **/ 缩略图**：数据，不是皮肤。
- **新任务默认色** `#8EB4E3`：产品默认，可以后再做成设置，本轮不进主题副本。
- **奖池/愿望单行内容、收藏图片像素**。
- **系统标题栏、MessageBox、文件选择器、系统取色板、托盘图标**。



### 设置页和副本

**位置（默认）：** **主窗口** 第六个页签「设置」。不另开设置窗（少一个 Owner/托盘关系）。实现时改 `窗口信息.md`。

**页内：**

- 当前样式：下拉（普通模式、黑夜模式、用户起的副本名）。
- 「另存为副本」：要一个名字，把当前参数抄一份；之后改的是这份，两个预设不被覆盖。
- 用户副本可改名、删除；两个预设不可删、不可在文件里改死值（若用户在预设上点了取色，等于先悄悄另存再改，或提示「预设只读，请另存」——见提问）。
- 每个颜色参数一行：色块 + 取色（复用现有系统取色板）+ hex。
- 字号、圆角、列数、**卡片高度 / 图区高度**、开始任务按钮尺寸/圆角：数字或滑条。
- **开始任务图标：** 预览小图 +「浏览…」选 PNG；清空则回到包内默认 `start-task.png`。
- 改完即时预览（当前窗口立刻套用，卡片按新高度重排）；关程序再开仍是上次那套。

**预设内容（落地时写死在程序里，不依赖 Noco）：**

- **普通模式：** 贴近现在的浅色：窗底接近白，表面 `#FAFAFA` 量级，字近黑，次要字灰，强调 `SteelBlue`，参考线浅灰，暂停中灰。
- **黑夜模式：** 窗底深灰（约 `#1E1E1E`），表面略亮（约 `#2D2D2D`），主文字浅、次要文字中灰，强调用更亮的蓝，边框/参考线变暗，暂停条提高亮度以免看不见。任务色不变。

切换普通/黑夜 = 把「当前样式」指到对应预设并 Apply。用户副本与预设是平级的可选项。

### 存在哪

**默认：工作区本地 JSON**（与 `playground/last-settings.json` 同类，例如 `Personal_Management/styles.json`：当前 id + 用户副本列表）。预设只在代码里。

不进 NocoDB：样式不是业务数据，启动准备窗还要在连上 Noco 之前就能上色。`app_state` 里只记抽奖次数等，不必塞一整份主题。

### 落地时注意（实现轮再做，本轮不动代码）

- 代码画的卡片/周视图必须走主题资源，不能继续写 `Brushes.SteelBlue`。
- 任务提醒窗、二级密码窗、启动准备窗同样。
- `DynamicResource` + 换资源字典即可热切换；打开的 **任务执行窗** 也要跟上。
- 任务页去掉 `UniformGrid` 占比行高；四墙统一固定 `card-height`。
- 新增 **奖励编辑窗**；任务/愿望编辑复用现有窗。改页签与跳转时同步 `窗口信息.md`。
- 开始任务图标打进输出目录；主题里的自定义路径要能解析相对工作区的路径。



### 提问

1. **设置放页签还是独立窗？**
  **默认：** 主窗口页签「设置」。
2. **在「普通模式」上改颜色，是拒绝，还是自动另存？**
  **默认：** 提示「预设不能改，请另存为副本」，不直接改预设。
3. **次要文字用独立** `text-secondary`**，还是继续** `text-primary` **+** `hint-opacity`**？**
  **默认：** 只要 `text-secondary`（普通/黑夜各给一个灰），去掉分散的 Opacity 魔法数。
4. `week-column-background` **是否合并进** `surface-background`**？**
  **默认：** 本轮分开，日历格可以和卡片底略有差别。
5. **副本文件放工作区还是** `%AppData%`**？**
  **默认：** 工作区（跟 nocodb 配置、需求文档在同一棵树，换机拷文件夹即带上皮肤）。
6. **要不要跟 Windows 系统深色模式自动同步？**
  **默认：** 不要。只认设置页当前选中的那套。
7. `card-columns` **放进主题副本是否容易误伤（黑夜变成每行 2 张）？**
  **默认：** 放进副本（布局也算样式）；两个预设都写成 3。
8. **失败色块要不要单独** `failed-overlay` **色，还是只用透明度打在任务色上？**
  **默认：** 透明度 + `border-subtle` 画虚线，不另开失败填充色。
9. **任务双击改成编辑后，工具栏「开始执行」还留不留？**
  **默认：** 留。和卡片播放键同一套 `StartOrFocusRunAsync`。
10. **奖励卡双击编辑，奖池条目以前只能种子数据。用户改权重会不会搞乱抽奖？**
  **默认：** 允许改。奖池本来就该能调；没有删除按钮（本轮不做删奖励）。
11. **奖励/愿望没有缩略图，顶区** `card-thumb-height` **空着是否浪费？**
  **默认：** 仍留顶区（浅底 + 类型字），四类卡一样高，墙面对齐。  
  用户意见：奖励/愿望可以设置缩略图。
12. **自定义开始任务图标复制进工作区，还是只记外部路径？**
  **默认：** 复制到 `Personal_Management/Desktop/Assets/user/` 再记相对路径，避免原文件被删后按钮空白。
13. **迭代 1 写过本轮不做任务编辑。本轮双击=编辑，是否包含改颜色/缩略图？**
  **默认：** 包含。编辑任务窗能改迭代 1 新建时那些字段（含颜色、缩略图）。



## 迭代 3

窗口名称仍按 `窗口信息.md`。本轮只定方案，不改代码。落地时在现有主题资源上补控件模板，设置页加对应颜色项。

### 目标

修黑夜模式下几处仍走系统浅色的控件，并把这些颜色收进主题：

1. 侧边滚动条（滑动块）现在发白，要能设成黑/深色，且在设置页可改。
2. 窗口最上方带最小化/最大化/关闭的那一栏（系统标题栏）发白——先查 Windows 常规软件能不能改，再决定本轮做不做。
3. 鼠标滑过「收藏夹」等页签时，签底变成白，白字看不清。页签文字一律用灰色；页签底、悬停底、选中底纳入主题，设置页可改。
4. 各页顶部功能栏（「新建任务」「开始执行」那一行）右侧多出一条滑动块：改成固定栏，不显示这条滑动块。所有滚动条都不显示上下两个箭头按钮。



### 现状（已查代码）

迭代 2 已经用 `DynamicResource` 刷了窗口底、按钮、输入框、`TabControl`/`TabItem`/`ToolBar`/`ScrollViewer` 的 Background/Foreground **Setter**。

没有自定义 `ControlTemplate`。WPF 默认模板内部仍用系统色：

- **滚动条：** `ScrollBar` 未设样式。Win10 默认 Aero 滑块/轨道偏浅，黑夜客户区里会看成一条白滑动块。模板两端各有一个 `RepeatButton`（LineUp / LineDown），就是上下箭头。
- **页签：** `TabItem` 只设了未悬停时的 Foreground/Background。悬停、选中由默认模板触发器改成系统浅色底，所以黑夜模式白字叠白底。
- **功能栏：** 各页顶部是 WPF `ToolBar`（`MainWindow.xaml`）。`ToolBar` 自带溢出区：按钮排不下或高度被撑开时，右侧会出现溢出钮/内部滚动条。这不是下面卡片 `ScrollViewer` 的那条，而是工具条自己的。收藏夹按钮多，最容易出现。
- **标题栏：** `MainWindow` 等仍是系统 DWM 标题栏（Title「个人管理」+ 右上角三个键）。WPF 资源刷不到这里。本机是 **Windows 10 22H2（19045）**。



### 可行性



#### 1. 滚动条改黑、去掉上下箭头

**可行，WPF 常规做法。** 给 `ScrollBar` 写一份 `ControlTemplate`：轨道 + 滑块，**不要** LineUp/LineDown 两个 `RepeatButton`。横条同样去掉左右箭头，避免两套观感。

颜色进主题（设置页可改）：


| 参数名               | 管什么    | 普通模式预设    | 黑夜模式预设               |
| ----------------- | ------ | --------- | -------------------- |
| `scrollbar-track` | 滚动条槽   | `#F0F0F0` | `#2A2A2A`            |
| `scrollbar-thumb` | 可拖的滑动块 | `#A0A0A0` | `#111111`（近黑，按第 1 条） |


应用到主窗口各 `ScrollViewer`、本周执行、设置页长列表，以及以后任何标准 `ScrollBar`。系统文件选择器/MessageBox 里的条改不了。

#### 2. 系统标题栏（退出键那一栏）

**结论：常规 Win32/WPF 窗口默认改不了任意颜色；能改的程度取决于系统版本和是否自绘标题栏。**


| 做法                                                                | 本机 Win10 19045          | 能改成什么样                                     | 代价                              |
| ----------------------------------------------------------------- | ----------------------- | ------------------------------------------ | ------------------------------- |
| 只改 WPF `Window.Background`                                        | 无效                      | 客户区变、标题栏仍系统浅色                              | —                               |
| `DwmSetWindowAttribute` 沉浸式深色标题栏（`DWMWA_USE_IMMERSIVE_DARK_MODE`） | **可用**（Win10 1809+）     | 标题栏变系统深色（接近资源管理器深色），不是任意色；最小化/最大化/关闭仍是系统按钮 | 几行 Win32，不换窗口结构                 |
| Win11 标题栏/文字着色（`DWMWA_CAPTION_COLOR` / `TEXT_COLOR`）              | **不可用**（这是 Win11 22H2+） | 本机用不上                                      | —                               |
| `WindowChrome` / `WindowStyle=None` 自绘标题栏                         | 可用                      | 底色、字色、关闭键都可跟主题走                            | 要自己画标题、拖动、双击最大化、高 DPI、非主窗口也要套一层 |


常见软件：资源管理器/记事本跟系统浅色标题栏；VS Code、不少 Electron/WinUI 应用是自绘标题栏才能贴主题色。

**本轮默认（未改）：** 不自绘。黑夜模式用沉浸式深色标题栏；普通模式系统浅色。设置页不提供标题栏取色。若改用 `WindowChrome`，见下一小节。

#### 2.1 用 `WindowChrome` 自绘标题栏（专项）

**结论：在当前 WPF / Win10 技术栈上可行，而且是标题栏颜色真正跟主题走的唯一稳妥办法。** 工作量中等，必须所有产品窗口一起换，不能只改主窗口。

`System.Windows.Shell.WindowChrome` 是 WPF 自带的（不必加包）。正确用法是 **保留** `WindowStyle=SingleBorderWindow`，挂上 `WindowChrome`（`GlassFrameThickness=0`，`UseAeroCaptionButtons=False`，`CaptionHeight` 留给自绘条），客户区顶部自己画一条标题栏。这样还走 DWM：拖到屏幕边缘吸附、Win+方向键、阴影、用任务栏缩略图。不要用 `WindowStyle=None` 当主方案——那会丢掉吸附和系统阴影，还要自己处理缩放边，坑比 Chrome 多。

和「沉浸式深色标题栏」对比：


|                | 沉浸式 DWM           | `WindowChrome` 自绘                           |
| -------------- | ----------------- | ------------------------------------------- |
| 本机 Win10 19045 | 能用                | 能用                                          |
| 颜色             | 只能系统深/浅，不能取色      | 底、字、按钮悬停都可进主题                               |
| 关闭/最小化键        | 系统按钮，浅色标题栏上仍是系统样式 | 自己画，可跟黑夜模式                                  |
| 主窗口关=藏托盘       | 现有 `Closing` 照旧   | 自绘关闭键调用 `Close()` 即可，仍进现有逻辑                 |
| 任务执行窗关=成功结束    | 同上                | 同上                                          |
| 吸附 / 最大化       | 系统的               | 保留（不要 `WindowStyle=None`）；最大化时要给任务栏留边，这是常见坑 |
| 覆盖窗口数          | 所有 HWND 自动带系统栏    | 每个 WPF `Window` 都要套同一套栏                     |


本程序要套的窗口（`窗口信息.md`）：主窗口、启动准备窗、任务执行窗、新建/编辑任务窗、愿望编辑窗、奖励编辑窗、收藏编辑窗、任务提醒窗、二级密码窗，以及设置里「另存为副本」那种代码拼的输入窗。系统 MessageBox / 取色板 / 文件选择器仍然是系统标题栏，改不了。

落地形态（若做）：

- 一份共用标题栏（标题字 + 主窗口的最小化/最大化/关闭；`NoResize` 对话框只留关闭）。按钮 `WindowChrome.IsHitTestVisibleInChrome=True`，命令用 `SystemCommands`。
- 主题参数：`title-bar-background`、`title-bar-text`、`title-bar-button-hover`、`title-bar-close-hover`。普通模式浅底深字，黑夜模式深底灰字，关闭键悬停可红。设置页可改。
- 高度固定（约 32 DIP），不进主题，避免每台 DPI 乱套。
- 代码里 `new Window` 的（启动准备、提醒、二级密码、名称提示）走同一个 `Theme.Tint` / 附加 Chrome 的入口，避免漏。

风险（都能处理，但要测）：

- 最大化客户区盖住任务栏：最大化时加边距或调整 `ResizeBorderThickness`。
- 只给主窗口上 Chrome、对话框仍系统白栏：观感比现在更裂，所以要全套。
- `UseAeroCaptionButtons=True` 会留下系统三键（Win10 上仍可能是浅色），等于没自绘，不要用。
- 高 DPI 一般按 DIP 没问题；要在 100%/150% 各拖一次、最大化一次。

工作量大约：共用标题栏 + 样式 + 十来个窗口各加一行附加属性，外加最大化边距。比滚动条/页签模板多，但仍在本仓库 WPF 能力内，不加第三方库。

**若选用 WindowChrome：** 迭代 3 提问 1 改为自绘；设置页追加上面四个标题栏颜色；沉浸式 DWM 方案不必做（自绘后系统标题栏已不显示）。系统对话框标题栏仍然白，文档里保持「改不到」。

#### 3. 页签悬停发白、文字改灰；背景可编辑

**可行。** 必须换掉默认 `TabItem` 模板（只改 Setter 压不住悬停触发器）。模板里绑定主题色，覆盖默认 / 悬停 / 选中。

页签文字：**所有状态都用灰色**（`tab-text`），不再用 `text-primary` 的近白，避免叠到浅底上看不清。


| 参数名                       | 管什么               | 普通模式                    | 黑夜模式              |
| ------------------------- | ----------------- | ----------------------- | ----------------- |
| `tab-text`                | 页签文字（默认/悬停/选中同一色） | `#555555`               | `#B0B0B0`（灰，不是纯白） |
| `tab-background`          | 未选中、未悬停           | 接近 `surface-background` | 同左                |
| `tab-hover-background`    | 鼠标滑过              | `#E8E8E8`               | `#3A3A3A`（不要白）    |
| `tab-selected-background` | 当前页签              | `#FFFFFF` / 接近窗底        | `#252525`（不要白）    |


设置页这四项都可改。`TabControl` 页签条底用 `window-background` 或 `tab-background`，不要另开「每个页签名一个色」。

#### 4. 功能栏右侧滑动块；滚动条不要上下箭头

**功能栏那条：可行。** 原因是 `ToolBar` 溢出/内部滚动，不是内容区 `ScrollViewer`。

落地默认：

- 工具条 **禁止溢出滚动**：按钮 `OverflowMode=Never`，并藏掉溢出箭头；高度锁在内容高，右侧不再出现滑动块。
- 收藏夹按钮多、窗口变窄时：按钮可以挤在一行（缩小间距）或换行；**默认换行**（`ToolBar` 换成 `WrapPanel` 一行按钮，没有溢出滚动）。不出现功能栏滑动块。
- 内容区滚动仍由下面的 `ScrollViewer` 负责（任务卡、收藏墙等）。

**所有滑动块去掉上下箭头：** 与第 1 条同一套 `ScrollBar` 模板，全局生效。

### 新增主题参数（相对迭代 2）

设置页「颜色」里追加：`scrollbar-track`、`scrollbar-thumb`、`tab-text`、`tab-background`、`tab-hover-background`、`tab-selected-background`。

不追加标题栏色（除非提问 1 改选 `WindowChrome`，那时加 `title-bar-*` 四项）。

### 不要纳入 / 改不到的

- 系统标题栏：默认仍只做沉浸式深色；若改选 `WindowChrome` 则全部产品窗口自绘，系统对话框除外。
- MessageBox、文件选择器、系统取色板里的滚动条和标题栏。
- 托盘菜单。



### 落地时注意（实现轮再做）

- `ScrollBar` / `TabItem` 要完整 `ControlTemplate`，`DynamicResource` 绑新参数；改主题即时刷新。
- 功能栏去掉 `ToolBar` 溢出或换 `WrapPanel`，六页都改（任务、本周、奖励、愿望、收藏、设置）。
- 沉浸式深色标题栏在主题 Apply 时按当前是不是黑夜模式开关；普通模式关回去。
- 改页签观感不必改 `窗口信息.md` 的跳转；若功能栏从 `ToolBar` 换成别的容器，内部控件名称没变则清单可只加一句「无溢出滚动」。



### 提问

1. **标题栏本轮做到哪？**
  **默认：** 只开系统沉浸式深色（黑夜模式）；不自绘、设置页不提供标题栏取色。  
   **备选：** 用 `WindowChrome` 自绘（见 2.1）。所有产品窗口统一标题栏，颜色进主题；系统对话框除外。
2. **滚动条滑块黑夜模式用近黑** `#111111`**，会不会和近黑轨道分不清？**
  **默认：** 轨道 `#2A2A2A`、滑块 `#111111`；若太糊，滑块改用 `#5A5A5A`（仍比现在的白块深）。设置页可自己再调。
3. **功能栏按钮换行，还是缩窗口时挤在一行（可能裁切）？**
  **默认：** `WrapPanel` 换行，宁可两行也不出滑动块。
4. **页签灰色字是否选中时也不加粗/不变色？**
  **默认：** 三种状态同一 `tab-text`；选中只靠 `tab-selected-background` 和稍厚的底边区分。



## 迭代 4

窗口名称仍按 `窗口信息.md`。本轮只定方案，不改代码。落地时改主题/控件模板、收藏卡尺寸、奖励添加入口、缩略图选区窗、主窗口尺寸落盘；程序图标等人工图。

### 目标

1. **程序图标：** 带厚度的像素五折。图要等人工绘制；代码只负责接到 exe / 窗口 / 托盘。
2. **「当前样式」下拉白底白字：** 设置页那个 ComboBox 要能跟主题走，设置页可改。
3. **页内选项栏悬停/焦点仍白底白字：** 统一收进主题：选中、鼠标划过时的**字体颜色**（背景一并配，否则只改字色仍可能糊）。同一套参数打在所有产品 ComboBox 上（设置页「当前样式」、收藏夹标签、各编辑窗类型/等级下拉），不要只修一个。
4. **收藏夹卡片文字区太大：** 收藏页单独一套卡片尺寸；文字只显示名称；**宽度用固定像素，不按窗口比例分列。**
5. **奖励页没有「添加」：** 功能栏补上，弹出已有的 **奖励编辑窗**（新建模式）。
6. **任务执行窗「暂停 / 失败 / 退出」不平行：** 先说明原因（见下），再改成一行、右对齐。
7. **选缩略图后加选区：** 任何卡片选图之后，在原图上框一块当缩略图。选区框位置和大小在窗里不动；用户缩放、拖动的是图。选区长宽比由该页卡片宽、图区高算出。
8. **主窗口尺寸：** 用户拖过之后关掉再开，还是那个大小。



### 现状（已查代码）

技术栈仍是 WPF `net8.0-windows`，无第三方 UI 库。迭代 3 已经给 `ScrollBar` / `TabItem` / `TabControl` 写了完整 `ControlTemplate`；`ComboBox` **没有**模板，只有 Background / Foreground / BorderBrush 的 Setter（`App.xaml`）。

- **图标：** `Desktop.csproj` 没有 `ApplicationIcon`。`Assets` 里只有 `start-task.png`。托盘用 `SystemIcons.Application`。窗口 Title 左侧也是系统默认。
- **ComboBox：** WPF 默认 Aero/Fluent 模板自己画关闭态的底、箭头、下拉列表。Setter 刷不进模板内部，黑夜模式常见结果是：**系统浅色底 +** `text-primary` **近白字**。设置页 `ThemeBox`（宽 180）、收藏夹 `FavTagBox`、新建任务的类型/等级、奖励/愿望/收藏编辑窗的类型，全是这一种。`ComboBoxItem` 同样没有 Style：下拉项悬停/选中走系统浅底。
- **收藏卡：** `CreateFavCard` 已经只把 `item.Title` 写进卡片（种类/来源在 ToolTip）。看起来「文字区巨大」，是因为高度仍用全局 `card-height`（预设约 200），图区用全局 `card-thumb-height`（约 96），下面空出一大截。宽度走 `CardItemWidth`：`(窗口客户区宽 / card-columns) - 间距`，和任务/奖励/愿望同一套，随窗口变宽变窄。
- **奖励：** 功能栏只有「抽一次」。`RewardEditWindow` 只有 `PrefillEdit`；`EditRewardAsync` 只 `Patch` 已有行。空奖池时种子四条（`SchemaService`），没有用户「添加」入口。迭代 2 提问 10 写过本轮不做删奖励。
- **任务执行窗：** `TaskRunWindow.xaml` 底栏已经是 `StackPanel Orientation="Horizontal" HorizontalAlignment="Right"`，三个按钮都是 `Width="88" Height="32"`。表面上就是「一行、右起」。不平行**不是**这三行 XAML 自己写错成换行，而是全局 `Button` Style 的 `Margin="0,0,8,4"` 只打在「暂停」上（见可行性第 6 条）。
- **缩略图：** 四个编辑窗都是 `OpenFileDialog` → 原文件路径 → `LoadLocalPreview` 整图缩小显示 → 上传整张原图当 `Thumb`。没有裁切、没有选区窗。卡片展示 `Stretch=UniformToFill`，实际显示等于「按卡宽×图区高再中心裁一块」，和用户选的整张图不是同一构图。
- **窗口尺寸：** `MainWindow.xaml` 写死 `Height="640" Width="980"`。没有读/写位置或大小。主题在 `Personal_Management/styles.json`，只记当前样式 id 和用户副本，不含窗口。关主窗口是藏托盘，进程不退，**同一次运行**里再打开还在；**退出进程再开**回到 640×980。托盘「打开主窗口」里还有 `WindowState = Normal`，最大化状态本来也不会被记住。



### 可行性



#### 1. 程序图标（等人工图）

**接线可行；图形本轮不画。** 人工交付一份多尺寸 `.ico`（至少 16 / 32 / 48 / 256，带透明）后：


| 位置                  | 做法                                                                         |
| ------------------- | -------------------------------------------------------------------------- |
| exe / 任务栏 / Alt+Tab | csproj `ApplicationIcon`，文件例如 `Personal_Management/Desktop/Assets/app.ico` |
| 各 WPF 窗口左上角         | `Window.Icon` 或 Application 级默认（与 ico 同源）                                  |
| 托盘                  | `NotifyIcon.Icon` 改成同一份，不要再 `SystemIcons.Application`                      |


像素五折、带厚度：这是美术，不是代码能「生成得像手绘」的东西。落地时若 ico 还不在，保持系统图标，路径和 csproj 先留好，文件一到就能挂。不要用 `start-task.png` 顶替产品图标。

系统 MessageBox / 文件选择器左上角图标跟 exe 走，接到 `ApplicationIcon` 之后一般会一起变。

#### 2–3. ComboBox 白底白字；悬停/选中字色进主题

**可行，必须换模板，和迭代 3 的页签同一类问题。** 只改 Setter 压不住默认模板里的悬停/焦点/下拉项。

给 `ComboBox` + `ComboBoxItem` 写 `ControlTemplate` / Style，绑定主题色。覆盖：

- 关闭态（设置页「当前样式」、收藏标签、各编辑窗下拉）
- 鼠标划过、键盘焦点（`IsMouseOver` / `IsKeyboardFocusWithin`）
- 下拉列表底、项的悬停、项的选中

颜色进主题（设置页可改）。字色单独开参数，避免再出现「底跟着主题走了、字仍是系统黑/我们的近白」。


| 参数名                         | 管什么             | 普通模式预设                            | 黑夜模式预设    |
| --------------------------- | --------------- | --------------------------------- | --------- |
| `combo-background`          | 关闭态底、下拉列表底      | `#FFFFFF`（近 `control-background`） | `#333333` |
| `combo-text`                | 关闭态文字、下拉项默认文字   | `#1A1A1A`                         | `#E8E8E8` |
| `combo-hover-background`    | 关闭态划过；下拉项划过     | `#E8E8E8`                         | `#3A3A3A` |
| `combo-hover-text`          | 上述划过时的字         | `#1A1A1A`                         | `#E8E8E8` |
| `combo-selected-background` | 下拉项当前选中（可与划过区分） | `#D6E6F5`（浅强调）                    | `#3A5A7A` |
| `combo-selected-text`       | 选中项的字           | `#1A1A1A`                         | `#E8E8E8` |


不必给每个下拉各开一套色。「当前样式」和「收藏标签」共用。箭头/边框用已有 `border-subtle` / `text-secondary`，不另开「箭头色」。

系统文件选择器、WinForms 取色板里的下拉改不了。

**不做本轮（同类但没点名）：** 普通 `Button` 悬停仍可能闪系统浅底。要修也是整份 Button 模板，工作量和 ComboBox 同级；本轮名单没有按钮，默认不动。若做完 ComboBox 仍觉得工具栏按钮刺眼，下一轮再套同一套 hover/selected 字色。

#### 4. 收藏夹卡片单独尺寸、只显示名称、固定宽度

**可行，只改收藏墙的布局参数，不动另外三墙。**

现状名称已经只有一行 `Title`。要缩小的是**卡片总高度里图下那条空带**，不是再删字段。

收藏页单独参数（设置页可改；两个预设写死默认值）：


| 参数名                     | 管什么                  | 预设（普通/黑夜相同）                |
| ----------------------- | -------------------- | -------------------------- |
| `fav-card-width`        | 收藏卡**内容宽**（像素，不含外边距） | 约 160                      |
| `fav-card-height`       | 收藏卡总高                | 约 148（图区 + 一行名称 + padding） |
| `fav-card-thumb-height` | 收藏卡图区高               | 约 110                      |


`CreateFavCard` 用这三项，**不要**再走 `CardItemWidth` / 全局 `card-height` / `card-thumb-height`。`WrapPanel` 仍然换行，只是每张卡宽度固定：窗口变窄就少一列，而不是把卡拉宽拉窄。

文字：继续只显示名称，`TextTrimming=CharacterEllipsis`，种类/来源留 ToolTip。字号仍用 `font-size-body`，不给收藏单独开字号。勾选框仍叠在图左上。

任务/奖励/愿望本轮仍按列数比例算宽（见提问）。选区比（第 7 条）收藏用 `fav-card-width`×`fav-card-thumb-height`（再扣卡片 Padding，落地时按实际图区外框算，避免框比墙上看到的更宽）。

#### 5. 奖励页「添加」

**可行，几乎是愿望单「添加」的复制。** `RewardEditWindow` 已有名称/类型/额度/权重/缩略图；缺的是新建模式（标题「添加奖励」、确定按钮「添加」、不 `PrefillEdit`）和主窗口 `CreateRecordAsync`。

- 奖励功能栏「抽一次」左侧（或右侧）加「添加」，与愿望单「添加」对称。
- 写入 `reward_pool`：`Title` / `Kind` / `QuotaAmount` / `Weight` / 可选 `Thumb`。
- **本轮仍不提供删除**（迭代 2 已定；本轮没要求删）。

实现时改 `窗口信息.md`：奖励页按钮列表 + 「添加」→ **奖励编辑窗**（新建）。

#### 6. 任务执行窗三个按钮不平行 —— 原因

**与 XAML 字面意思不相符，不是「本来就设计成错开」。**

底栏代码的字面意思是：一行横排、右对齐、三颗同样 88×32 的按钮。「暂停」没写 `Margin`，「失败」「退出」写了 `Margin="8,0,0,0"`。

但 `App.xaml` 里有一条**全局**按钮样式：`Margin="0,0,8,4"`（右边 8、下边 4，给各页功能栏 `WrapPanel` 换行用的）。

WPF 里元素自己写的 `Margin` 会**整份覆盖**样式里的 Margin，不会和样式相加：


| 按钮  | 实际 Margin            | 后果                                |
| --- | -------------------- | --------------------------------- |
| 暂停  | `0,0,8,4`（吃到全局样式）    | 比另外两颗**矮一截贴底**：多 4px 下边距；右侧还多 8px |
| 失败  | `8,0,0,0`（本地写死，样式无效） | 无下边距                              |
| 退出  | `8,0,0,0`            | 无下边距                              |


横排 `StackPanel` 的高度按最高的子元素算（暂停那颗带了 4px 下边距，槽高变成 36）。另外两颗高度锁死 32，在多出来的 4px 里按默认垂直对齐摆，看起来就不在一条线上。这不是 `DockPanel` 把按钮挤到下一行，也不是 `NoResize` 窗口 420 宽不够排（88×3 + 间距远小于客户区）。

**预期内的是：** 全局按钮 Margin 方便主窗口功能栏。**不符合表面意思的是：** 任务执行窗这三颗应当等大、一条基线，却没盖掉那条全局 Margin。

落地：三个按钮用同一套本地 Margin（例如一律 `8,0,0,0`，第一颗左 0），`VerticalAlignment=Center`；或底栏用 `Grid` 三列、右对齐。不必改成两行。不要为此取消全局按钮 Margin（功能栏还靠它换行）。

#### 7. 选缩略图后的选区（缩放/移动图，框不动）

**可行，WPF 常规：固定视口 + 图做缩放/平移变换。** 不必加库。

新窗 **缩略图选区窗**（落地写入 `窗口信息.md`）：

- 中间一块**固定大小、固定位置**的选区框（半透明遮罩 + 中间镂空，或只画矩形框）。框的长宽比 = 该次选图对应的「图区宽 : 图区高」。
- 框底下是原图。滚轮缩放、拖动平移；**不要**让用户拖框。缩放下限：图必须盖住选区（不许露出空白）；上限约 8×。
- 确定：按选区从原图像素裁出一块（`CroppedBitmap` 或把视口 `RenderTargetBitmap`，注意 DPI），写成临时 PNG，作为之后上传的 `ThumbPath`。取消：当没选这张图。
- 入口：任务 / 奖励 / 愿望 / 收藏 四个编辑窗里点「浏览…」选中文件之后；任务页拖入/Ctrl+V 预填缩略图之后。编辑时若用户不改已有缩略图，不弹选区。收藏的**主文件**（`File`）仍存原图；选区只作用于 `Thumb`。任务/奖励/愿望没有单独原图字段，**默认只把裁切结果存进** `Thumb`（再裁要重新选文件）。

长宽比：

- 收藏：`fav-card-width` 与 `fav-card-thumb-height`（扣 Padding 后与墙上图区一致）。
- 任务/奖励/愿望：本轮卡宽仍随窗口变的话，选区比用「当前该墙算出来的卡宽」（`CardItemWidth`）× `card-thumb-height`。窗口以后拉过，墙上 `UniformToFill` 可能和当时裁的构图略有出入——这就是条目里写「以后卡片宽度固定」的原因。本轮若只固定收藏宽，其它三墙接受「按打开选区时的卡宽来比」。

拖入一张已经很小的图：允许放大到盖住选区。GIF/WebP：裁切后变静态 PNG 即可（现有预览也不是动图）。

#### 8. 主窗口尺寸持久化

**可行。** 记 `Width` / `Height` / `Left` / `Top` / `WindowState`（Normal 或 Maximized；不要把 Minimized 写盘）。

**存在哪（默认）：** 工作区本地 JSON，与主题分开，例如 `Personal_Management/window.json`。不要塞进 `styles.json`（换皮肤不该丢掉窗口大小），也不进 Noco（启动准备窗还没连库，主窗口一出来就要还原）。

还原时机：创建 `MainWindow` 后、`Show` 前读文件；超出当前屏幕工作区则夹紧（换显示器、插拔扩展屏）。最大化时先还原 Normal 的宽高位置再设 Maximized，避免下次以最大化像素当 Normal 尺寸。

写入时机：`SizeChanged` / `LocationChanged` 防抖，以及藏托盘、托盘退出时各写一次。托盘「打开主窗口」不要无条件 `WindowState = Normal` 把最大化冲掉；若用户上次是最大化，打开仍应最大化。

**只持久化主窗口。** 任务执行窗是 `NoResize` 固定 420×280；各编辑窗、提醒、二级密码按内容，不记。启动准备窗是临时窗。

### 新增主题 / 布局参数（相对迭代 3）

设置页颜色追加：`combo-background`、`combo-text`、`combo-hover-background`、`combo-hover-text`、`combo-selected-background`、`combo-selected-text`。

设置页数字追加：`fav-card-width`、`fav-card-height`、`fav-card-thumb-height`。

窗口矩形不进主题副本。程序图标不是主题参数。

### 不要纳入 / 改不到的

- 像素五折图形本身（等人工）。
- 系统对话框里的 ComboBox / 标题栏 / 滚动条。
- 本轮不改普通按钮悬停模板（除非提问改选）。
- 本轮不删奖励、不给任务/奖励/愿望改固定宽度（除非提问改选）。
- 不把原图和裁切矩形分两套字段存 Noco（除非提问改选）；默认 `Thumb` 就是裁切后的图。
- 其它窗口的尺寸。



### 落地时注意（实现轮再做）

- ComboBox 模板要含 `PART_EditableTextBox` 以外的标准部件（本程序下拉都不可编辑，用非 Editable 模板即可），下拉 `Popup` 底绑 `combo-background`。
- 全局 `Button.Margin` 保留；只在任务执行窗覆盖。
- 收藏墙不要再调用 `CardItemWidth`；`CardHost_SizeChanged` 对收藏仍可重画，但宽度不再随 `e.Width` 变。
- 选区窗走 `Theme.Tint` + 沉浸式标题栏，与其它产品窗一致。
- 改功能栏、新窗、奖励新建跳转时同步 `窗口信息.md`。
- `window.json` 坏了或没有就回退 980×640。



### 提问

1. **程序图标本轮怎么办？**
  **默认：** 代码留 `Assets/app.ico` 挂接点；文件未交付则仍用系统图标，不拿播放键顶替。
2. **ComboBox 六色是否和页签色合并？**
  **默认：** 分开。页签已经是灰字+厚底边；下拉需要可选中字色，绑在一起不好调。
3. **收藏三参数的预设大概 160×148、图区 110，是否要改数字？**
  **默认：** 按上表；设置页可调。
4. **任务/奖励/愿望本轮是否也改成固定卡宽？**
  **默认：** 不。只有收藏固定宽；那三墙仍 `card-columns` 比例宽。选区比对「打开选区当时的卡宽」。
5. **裁切结果怎么存？**
  **默认：** 只上传裁切后的图当 `Thumb`。收藏主文件仍是原图。
6. **主窗口是否连位置、最大化一起记？**
  **默认：** 宽高 + 位置 + 最大化都记。最小化不记。
7. **奖励要不要删除？**
  **默认：** 不要。只要「添加」。
8. **任务执行窗三个按钮：是否同意是全局 Margin 误伤，本轮只改这一处对齐？**
  **默认：** 是。一行右对齐保持不变，盖掉全局 Margin。



## 迭代 5

窗口名称仍按 `窗口信息.md`（落地时：「本周执行」改为「日程记录」；任务卡加「记录任务」；各墙归档与「显示已归档」）。本轮先定方案与修漏，落地实现时再改代码。

### 目标

1. **所有卡片固定宽**（不再按窗口比例分列）。收藏页迭代 4 已固定；任务 / 奖励 / 愿望同样改。
2. **修：选缩略图一律「无法读取这张图片」。**
3. **查：编辑卡片时顶部栏先白底白字、字上有黑高亮；点别处再点回编辑窗才正常。** 是否符合当前代码预期，要不要手动刷新。
4. **任务卡「开始任务」旁加红色菱形「记录任务」：** 把「上一个任务结束 → 现在」记成当前任务的一次执行。结算接口必须复用现有执行结束逻辑，不要另写一套发奖。图标用 Python 画进 `Assets`。
5. **任务归档：** 归档后默认不出现在任务页；勾选「显示已归档内容」才见。归档任务也不进「日程记录」页。
6. **页签「本周执行」改名为「日程记录」。**
7. **奖励卡、愿望卡同样可归档 + 「显示已归档内容」。**
8. **溢出进度：** 任务卡缩略图右上角纵向进度条（自下而上）；悬停「溢出进度累计：23/30 min」。编辑窗增加「奖励要求时长」（分钟）和「允许溢出进度」。改时长或取消勾选且溢出非零时要确认并清零。退出/记录任务时按规则把余数滚进溢出，满一轮额外结算，可连结多次。执行窗进度条：要求时长满后同条再用金条、红条轮流重跑。溢出存浮点，悬停显示四舍五入整数。



### 漏洞检查（已查代码）



#### 2. 「无法读取这张图片」——bug，不是用户选错文件

文案只出现在 `ThumbCropWindow.OnLoaded`：`FavoriteService.LoadLocalBitmap` 返回 null。`Ask` 已经确认文件存在且扩展名在 `png/jpg/jpeg/gif/webp/bmp` 里，所以不是「没选到文件」。

`LoadLocalPreview`（卡片预览、开始任务图标）同一套 `BitmapImage` + `StreamSource` + `OnLoad`，**能**读常见 jpg/png。`LoadLocalBitmap`（选区窗专用）几乎相同，但多了两处：


|                    | `LoadLocalPreview` | `LoadLocalBitmap`      |
| ------------------ | ------------------ | ---------------------- |
| `DecodePixelWidth` | 有（缩小）              | 无（整图）                  |
| `CreateOptions`    | 默认                 | `**IgnoreImageCache**` |
| 失败                 | catch 后 null       | 同上，**异常被吞掉，没有日志**      |


`IgnoreImageCache` 是给 **Uri 缓存** 用的。选区走的是 `File.OpenRead` 的 `StreamSource`，没有 Uri。在 WPF / WIC 上这个组合会让 `EndInit` 抛错，catch 之后就是现在的现象：**任务 / 奖励 / 愿望 / 收藏，选什么图都失败。** 这和「某一类卡片写错路径」无关，四条入口都进同一个 `Ask`。

次要坑（修完主因后仍可能碰到）：

- 本机 **Win10 19045 的 WIC 不解 WebP**。筛选器里写了 `*.webp`，WebP 会继续失败。GIF/JPEG/PNG/BMP 应能过。
- `File.OpenRead` 默认 `FileShare.Read`，文件若被资源管理器预览锁住，偶发失败。更稳是先 `ReadAllBytes` 进 `MemoryStream`。
- 失败时选区窗已经 `Show` 再弹 MessageBox，体感像闪一下。

**结论：不符合预期，要修。** 落地：去掉 `IgnoreImageCache`；bytes → MemoryStream；catch 里至少 `Debug` 打异常。WebP 从选图筛选器拿掉，或失败时说明「不支持 WebP」。不要在选区窗里用 `DecodePixelWidth` 缩太小（裁切要够分辨率）；超长边仍可限制 4096。

#### 3. 编辑窗顶部栏白底白字 / 黑高亮 ——bug，不是设计如此

「顶部栏」对应对话框 **系统标题栏**（Title + 右上角关闭），不是表单第一行。现象和迭代 3 的 DWM 沉浸式深色标题栏对得上：

- 主窗口在 `InitializeComponent` 之后立刻 `Theme.Tint` → `CaptionTheme.Apply`。此时还没有 HWND，于是挂上 `SourceInitialized`，**第一帧之前**就能 `DwmSetWindowAttribute`。
- **新建/编辑任务、奖励、愿望、收藏** 都是 `Loaded += Tint`。顺序是：`ShowDialog` → `SourceInitialized`（此时还没人订 DWM）→ **先画出系统浅色标题栏** → `Loaded` 才 Tint、才设深色标题栏。
- Win10 19045 上，窗口已经画过之后再改 `DWMWA_USE_IMMERSIVE_DARK_MODE`，**标题栏往往不重绘**，直到失焦再聚焦（点别处再点回来）——与描述一致。
- 黑夜模式客户区字是近白；标题栏仍浅底时，DWM 可能已经按「深色标题」去画白字 → **白底白字**。所谓「黑色高亮」是浅底上白字的 ClearType / 描边残留，不是业务里另做的高亮。

次要：`PrefillEdit` / `PrefillTitle` 在 `ShowDialog` 前对名称框 `Focus` + `SelectAll`。`TextBox` 没有自定义模板，选区色走系统（常是深蓝/黑底）。若用户指的其实是名称框而不是标题栏，那是选中态系统色；点别处取消选中就「恢复」。和标题栏问题可以同时存在。

**结论：不符合当前代码的「表面意思」（Tint 了就该整窗深色），也不需要用户手动 repaint。** 不要靠 `InvalidateVisual` 碰系统标题栏。落地：所有产品 `Window` 在构造函数里 `InitializeComponent` 之后就 `Theme.Tint`（与主窗口相同），保证 `SourceInitialized` 时 DWM 已订阅；必要时在 `SourceInitialized` 末尾再 `Apply` 一次。`Loaded` 里可保留 Tint（换主题时），但不能只靠 Loaded。名称框的系统选区色本轮不必做 TextBox 模板（除非提问要求）。

### 现状（功能相关，已查代码）

- **卡宽：** 任务/奖励/愿望 `CardItemWidth` = `(客户区宽 / card-columns) - 间距`。只有收藏用 `fav-card-width`。选区比对非收藏墙仍问 `MeasureCardWidth` → 比例宽。
- **执行与发奖（可复用，不要复制）：**
  - 开跑：`StartOrFocusRunAsync` 插入或恢复 `sessions`（`StartedAt`、`Task` 关系），全局只开一个 `TaskRunWindow`。
  - 结束：`TaskRunWindow.FinishAsync` → `OnRunFinishedAsync`：Patch `EndedAt` / `Outcome` / `PausedSeconds` / `PauseJson`；`SessionLogic.ActiveSeconds` + `RewardUnits`（写死 **1800 秒**）；成功且至少 1 个单位才写当天 `completions`、按次调用 `RewardLogic.TicketsForCompletion`、加钱包。
  - 失败 / 托盘退出：`CompleteFromOutsideAsync(false)` / `AbandonRunningAsync`，不发奖。
  - `SessionLogic.UnitSeconds`、执行窗文案和提醒，全部写死「30 分钟」。
- **没有：** `Archived`、奖励要求时长、溢出字段、「记录任务」、金/红进度条。
- **日程页：** 页签 Header 仍是「本周执行」；按当前自然周画 `sessions`，不看任务是否归档。



### 可行性

**可行，仍是现有 WPF + Noco 列。** 不换框架。结算必须收成一个函数，执行窗退出和「记录任务」共用。

#### 1. 全部卡片固定宽

与收藏同一套路：`WrapPanel` + 像素宽，窗口变窄就少一列，不拉长卡片。

设置页增加（任务/奖励/愿望共用，两个预设相同）：


| 参数名          | 管什么          | 预设                |
| ------------ | ------------ | ----------------- |
| `card-width` | 三墙内容宽（不含外边距） | 约 200（接近现在三列时的观感） |


收藏继续用已有 `fav-card-width`，不要合并（收藏更窄）。`card-columns` 不再参与宽度；设置页本轮藏掉或标「已不用」，避免再改出「每行张数」。选区比：非收藏用 `card-width` − padding × `card-thumb-height`（与迭代 4 收藏算法一致）。`CardHost_SizeChanged` 里按宽度重画三墙的逻辑可去掉（宽已固定，只需主题变更时重画）。

#### 4. 「记录任务」+ 复用结算

**含义（默认）：** 取 **全局最近一条已结束 session** 的 `EndedAt`（成功或失败都算「上一个任务结束」），到现在为止的墙钟时间，记成 **当前这张卡** 的一次成功执行：无暂停、不打开任务执行窗。

可复用（落地时抽出来，禁止再写一份发奖循环）：


| 现有                                                                      | 记录任务怎么用                                                        |
| ----------------------------------------------------------------------- | -------------------------------------------------------------- |
| `sessions` 的 Create + Patch（时间、Outcome、PausedSeconds、PauseJson、Task 关系） | 新建一行：`StartedAt=上次EndedAt`，`EndedAt=现在`，`Outcome=success`，暂停 0 |
| `SessionLogic.ActiveSeconds` / 按「要求时长」算单位（本轮改掉写死的 1800）                 | 同上                                                             |
| `OnRunFinishedAsync` 里 completions + `TicketsForCompletion` × 单位 + 钱包   | 抽成 `SettleSuccessAsync(task, activeSeconds, …)`，溢出也放这里         |
| 「已有执行窗则不准第二路」                                                           | 正在跑时点记录：与点第二张播放键相同，聚焦执行窗并提示，**不**另记一条                          |


不要复用：`TaskRunWindow` 的计时器/暂停 UI；不要假装用户点过暂停。

没有任一条带 `EndedAt` 的 session：MessageBox「还没有上一次任务结束时间」，不写库。

图标：Python 画 `Personal_Management/Desktop/Assets/record-task.png`（与 `start-task.png` 同尺寸量级，红菱形、圆角外透明），csproj 复制到输出目录。叠在播放键 **左侧**（仍在缩略图右上，避免挡纵向溢出条）。点按钮 `e.Handled = true`，不要冒泡成双击编辑。工具栏本轮不强制加「记录任务」（卡上入口足够）；若要备份可再问。

#### 5 / 7. 归档

Noco 三表各加 `Archived`（Checkbox，缺省 false）。`SchemaService.EnsureColumnAsync` 与 Thumb 一样启动时补列。

- 任务 / 奖励 / 愿望编辑窗各一个「已归档」勾选，保存时写入。
- 三页功能栏各一个「显示已归档内容」（默认不勾）。不勾：列表过滤 `Archived != true`。勾了：全显示，已归档卡可用次要字或降低透明度区分（默认略降 Opacity，不另开主题色）。
- **日程记录永远不画归档任务的色块**，与任务页勾选无关。
- 本轮不做「删除」；归档就是从默认墙消失。
- 收藏不做归档。

进行中的任务被归档：执行窗仍可结束；结束后日程页不再画它。默认允许。

#### 6. 改名

主窗口 `TabItem Header`「本周执行」→「日程记录」。`窗口信息.md` 文档用名一起改。代码里 `WeekHost` / `LoadWeekAsync` 可保留，不必重命名符号。

#### 8. 溢出进度与「奖励要求时长」

`tasks` 新列：


| 列                 | 类型       | 默认         |
| ----------------- | -------- | ---------- |
| `RewardMinutes`   | Number   | 30（1–1440） |
| `AllowOverflow`   | Checkbox | false      |
| `OverflowSeconds` | Number   | 0（浮点秒）     |


旧行没有这些列时：时长当 30，不允许溢出，溢出 0。

**结算（成功退出或记录任务；失败不加溢出、不发奖）：**

设要求秒 `R = RewardMinutes * 60`（至少 1 分钟）。`active` 为扣除暂停后的有效秒。

1. `units = floor(active / R)` → 与现在 `RewardUnits` 相同，只是 R 不再写死 1800。
2. `remainder = active - units * R`。
3. 若允许溢出：`bank = OverflowSeconds + remainder`；`extra = floor(bank / R)`；`OverflowSeconds = bank - extra * R`；**总发奖次数 = units + extra**（`TicketsForCompletion` 调这么多次）。一次退出 `extra` 可以 > 1。
4. 若不允许溢出：不改 `OverflowSeconds`（应为 0），总次数 = `units`，余数丢掉。
5. `units + extra == 0` 时仍要写回溢出（第 3 步），completions 仍按「当天第一次成功且总次数 ≥ 1 才打卡」——**只有发奖次数 ≥ 1 才写当天 completion**（与现在「至少 1 个 30 分钟单位」一致）。仅把余数存进溢出、一次整单位都没有：不打卡、不发奖。

改「奖励要求时长」，或勾选「允许溢出进度」→ 取消勾选：若当前溢出 > 0，先 `MessageBox` Yes/No：「改动奖励要求时长将会使溢出进度清零，请问是否确定？」（取消勾选用同一句，见提问）。Yes：溢出置 0 再保存；No：不改这项。

输入时长非整数或不在 1–1440：MessageBox「输入的数据不符合要求」，不关窗。

**任务卡条：** 仅 `AllowOverflow == true` 时显示。贴在缩略图 **右侧内沿**，纵向，填充自下而上，`fill = OverflowSeconds / R`（0–1，满了仍显示满，因为结算后余数 < R）。不要挡菱形/播放键：条在最右，两个按钮在其左。ToolTip：`溢出进度累计：{round(overflow/60)}/{RewardMinutes} min`（四舍五入到整数分钟）。

**执行窗进度条：** `Maximum` 改为该任务的 R。`lap = floor(active / R)`，`frac = active - lap * R`。`Value = frac`（满格后从 0 再涨，即「再跑一遍」）。颜色：`lap == 0` 用现有 `Accent`；`lap` 为奇数金 `#D4A017`；偶数（≥2）用 `Danger`。金/红本轮不进设置页。到点提醒改为「已满 N 分钟」（N = 奖励要求时长），仍只在 **第一圈跑满** 弹一次。文案里的「30 分钟」全部改成该任务的 N。

执行窗构造时要带上 `RewardMinutes` 和 `AllowOverflow`（不要再读全局常数）。

`SessionLogic.UnitSeconds` 改为按参数传入，或删掉常数，避免和任务字段两套真相。

### 新增主题 / 库表（相对迭代 4）

- 主题：`card-width`。金/红进度不进主题。
- Noco：`tasks.RewardMinutes` / `AllowOverflow` / `OverflowSeconds` / `Archived`；`reward_pool.Archived`；`wishlist.Archived`。
- 资源：`Assets/record-task.png`（Python 生成）。



### 不要纳入 / 改不到的

- 收藏归档、翻周、WebP 解码库。
- 系统标题栏任意取色（仍是沉浸式深色；本轮只保证 **Show 前** 就 Apply）。
- 失败执行计入溢出。
- 为溢出单独做 Noco 日志表（余数只存在任务行上）。



### 落地时注意（实现轮再做）

- 修 `LoadLocalBitmap` 与编辑窗 `Tint` 时机，应和功能同一轮交，否则选区和黑夜标题栏仍坏。
- `SettleSuccessAsync` 单一入口：执行窗 `Finished`、记录任务、（若有）外部 Complete 成功。
- 改任务字段时确认框在 **编辑窗点保存时** 做（能对比旧溢出）；不要一改滑条就弹。
- 新建任务默认：时长 30、不允许溢出、未归档。
- 同步 `窗口信息.md`：页签改名、记录任务、归档勾选、选区修漏不必新开窗。



### 提问

1. **任务/奖励/愿望是否共用一个** `card-width`**（收藏仍用 fav 宽）？**
  **默认：** 是。预设约 200。
2. `**card-columns` 设置项？**
  **默认：** 本轮从设置页拿掉（宽已固定，再留「每行张数」会误导）。
3. **「上一个任务」指全局最近一次结束，还是这张卡自己上次结束？**
  **默认：** 全局最近一条已结束 session。
4. **正在执行时点「记录任务」？**
  **默认：** 不允许，与第二路执行相同。
5. **归档入口只要编辑窗勾选，还是再加「归档所选」按钮？**
  **默认：** 编辑窗勾选 + 三页「显示已归档内容」足够。
6. **取消「允许溢出」时确认文案是否与改时长共用那一句？**
  **默认：** 共用，少一种对话框。
7. **未满一个要求时长的成功退出，余数是否仍进溢出？**
  **默认：** 若已勾选允许溢出，要进；不发奖、不打卡。
8. **金/红是否进主题？**
  **默认：** 不进。
9. **工具栏要不要「记录任务」备份按钮？**
  **默认：** 不要，只留卡上菱形。



## 迭代 6

窗口名称仍按 `窗口信息.md`（落地时：日程块布局与删除、奖励编辑改「概率/基础奖励」、主题追加 ToolTip/按钮悬停/溢出条/日程小时高度）。本轮先定方案与修漏，落地实现时再改代码。

### 目标

1. **ToolTip 白底白字：** 检查是否符合代码预期；纳入主题；黑夜模式默认黑底白字。
2. **「任务」页签与「新建任务」按钮样式不一致（含悬停）：** 用同一套参数控制。
3. **设置页「取色…」** 与上条同一套按钮参数。黑夜模式取色钮白底白字：检查是否符合代码预期。
4. **日程记录块：** 占本列一半宽、靠右；块上沿/下沿齐平处、列左半居中画起止时间；两时间标签之间居中画任务名。
5. **设置页新增**「日程记录中每个小时对应的高度/px」。
6. **溢出进度条改样式：** 缩略图**左侧**；左上角距缩略图左/上边界各 5px；条高 = 缩略图高度一半；背景四角圆角、不透明、可主题调节；进度金色、可主题调节。
7. **日程记录可删：** 点选记录块 → 高亮边框（文字不高亮）→「删除」→ 确认框（含该块描述）→ 删库并刷新。
8. **奖励随机算法：** 百分比概率（未归档合计 ≤ 100%）；可勾「基础奖励」（动态 `100% − 其余`）；去掉权重；全局至多一个基础奖励；另设基础时旧基础变非基础且概率 0%，保存时提示；卡片上显示当前概率（基础奖在刷新时重算）。



### 漏洞检查（已查代码）



#### 1. ToolTip 白底白字 —— 符合「没做主题」的预期，不是业务写死白字

全仓库 **没有** `TargetType="ToolTip"` 的 Style/Template，主题里也没有 tip 色。`ToolTip="…"`（任务播放/记录、溢出条、日程块、收藏卡、若干编辑窗字段）走 WPF 默认：系统 `Info` / `InfoText`，**不**跟 `SurfaceBackground` / `TextPrimary`。

黑夜模式下客户区已是深底浅字，系统气泡仍常是浅底；若系统 Info 文本也被调成浅色，就会出现「白底白字」或极难读。这不是某条业务把 Foreground 设成白，而是 **悬浮提示从未进主题**。

**结论：符合当前代码预期（等于没管），要改。** 落地：`App.xaml` 给 `ToolTip` 写 Style（Background / Foreground / Border；可选 Padding）；主题加例如 `tooltip-background` / `tooltip-text`（普通近浅底深字；黑夜近深底浅字）。设置页两行取色。不必给每个控件手写 Tip 模板。

#### 2 / 3. 页签 vs 按钮 / 取色钮 —— 两套控件，悬停路径不同；取色白底白字是预期内的坏结果


|       | 「任务」TabItem                      | 「新建任务」/「取色…」Button                      |
| ----- | -------------------------------- | --------------------------------------- |
| Style | 完整 `ControlTemplate`（`App.xaml`） | **只有 Setter**，无模板、无 `IsMouseOver`       |
| 闲置底   | `TabBackgroundBrush`             | `SurfaceBackgroundBrush`                |
| 闲置字   | `TabTextBrush`                   | `TextPrimaryBrush`                      |
| 悬停    | `TabHoverBackgroundBrush`        | **系统 Aero/Fluent 浅灰底**，字仍 `TextPrimary` |


设置页 `ColorRow` 里 `new Button { Content = "取色…" }`，无局部 Style，与工具栏按钮同一隐式 Style。黑夜闲置：表面深 + 主字浅 → 可读；一悬停系统刷浅底、字仍浅 → **白底白字**。与迭代 4 改 ComboBox 前是同一类问题。

目标写「用同一个参数控制」页签与按钮：若字面共用 `tab-*`，语义别扭（页签还有选中态）。**建议：** 给 Button 补完整模板（闲置/悬停/按下），主题加 `button-background` / `button-text` / `button-hover-background` / `button-hover-text`（及可选 pressed）；预设里按钮闲置可对齐表面+主字，悬停对齐现在的页签悬停观感。页签继续用已有 `tab-`*。若坚持「同一组参数」，则页签悬停与按钮悬停都绑到同一对 brush（设置页只暴露一套「控件悬停」）——见提问。

**结论：不一致与取色悬停白字都符合当前代码；要修 Button 模板，不是用户误操作。**

### 现状（功能相关，已查代码）

- **日程：** `RenderWeekBoard` 内 `const pxPerHour = 22`（硬编码）；色块 `Canvas.SetLeft(3)`，宽 ≈ 列宽 − 6（几乎整列）；**无**块上时间/任务名文字（只在 ToolTip）；`_weekSpans` **不带 session Id**；无选中、无删除按钮；`NocoClient` 无 `DeleteRecordAsync`（仅 Create/Patch/List）。
- **溢出条：** 仅 `AllowOverflow` 时画在缩略图 **右侧** 内沿；宽 6、高 = 整段 `CardThumbHeight`；槽色写死半透明黑；填充 `AccentBrush`；圆角 2。与目标（左侧、高一半、主题色、金进度）都不符。
- **抽奖：** `Draw_Click` 用 `Weight` 展开袋子随机；**不读** `Archived`（归档奖仍可能被抽到）；无百分比、无基础奖励。编辑窗仍是「权重 ≥ 1」。种子奖权重 5/4/2/2。
- **主题：** 有周列底/参考线/暂停色；**无** 小时高度、ToolTip 色、按钮悬停色、溢出条底/金。



### 可行性

**可行，仍是现有 WPF + Noco。** 不换框架。删 session 要补 Delete API；抽奖要从权重制换成百分比 + 基础奖。

#### 1. ToolTip 主题

见漏洞 1。预设：


| 参数                   | 普通               | 黑夜                      |
| -------------------- | ---------------- | ----------------------- |
| `tooltip-background` | 近 `#FFFFF0` 或表面色 | 近 `#2D2D2D` / `#1E1E1E` |
| `tooltip-text`       | 近主文字深色           | 近主文字浅色                  |


可选 `tooltip-border` 跟 `border-subtle`。

#### 2 / 3. 统一按钮观感

Button 必须换模板（Setter 压不住悬停）。设置页「取色…」、工具栏「新建任务」等自动吃隐式 Style。页签是否与按钮共用悬停色：见提问；技术上都只是 brush 绑定。

#### 4 / 5. 日程块布局 + 小时高度

- 主题 `week-px-per-hour`（Number，设置页「日程记录中每个小时对应的高度/px」）；替换 `const 22`；`Clamp` 合理范围（如 8–80），预设 22。
- 每列 Canvas：右半画块（`Left = width/2`，`Width = width/2 − 边距`）；左半垂直栈：上时间（与块顶齐）、任务名（两时间中点居中）、下时间（与块底齐）。短块（高度不够塞三行字）时缩小字号或只显示 ToolTip——见提问。
- 失败态仍用淡填充/虚线框；高亮选中用 `Accent` 描边，**不要**给时间/任务名换强调色。



#### 6. 溢出条

`CreateTaskCard`：条放缩略图 Grid **左侧**；`Margin` 左/上 5；高 = `thumbH / 2`；宽保持细条（或主题可选，本轮可写死约 6–8）。背景 / 填充改 `overflow-track` / `overflow-fill`（填充预设金 `#D4A017`，与执行窗金条同色量级）；四角圆角；不透明。播放/记录钮仍在右上，不再为右侧条让位。

#### 7. 删除日程块

1. `NocoClient.DeleteRecordAsync(tableId, id)`：Noco v2 `DELETE /api/v2/tables/{id}/records`（body 带 Id 数组，与现有 Patch 风格一致）。
2. `_weekSpans` 增加 `SessionId`（及标题、起止、Outcome，供确认文案）。
3. 点击色块选中（再点可取消或点空白取消）；功能栏加「删除」；确认 MessageBox 文案含任务名与时间段；确认后 Delete → `LoadWeekAsync`。
4. 只删 `sessions` 行；**不**回滚当时的 `completions` / 钱包 / 溢出（本轮不做撤销发奖）。见提问。



#### 8. 奖励百分比 + 基础奖励


| 列             | 类型       | 说明                        |
| ------------- | -------- | ------------------------- |
| `Probability` | Number   | 0–100；非基础奖的固定百分比          |
| `IsBase`      | Checkbox | 至多一个未归档行为 true            |
| `Weight`      | （旧）      | UI 去掉；抽奖不再读。列可留着不删，避免折腾旧库 |


规则（落地按此实现）：

- 约束只统计 **未归档** 且 **非基础** 的 `Probability` 之和 ≤ 100；超了则保存失败（MessageBox，不关窗或关窗后拒绝写入——与现有校验一致即可）。
- 基础奖不写死百分比：展示与抽奖用 `100 − sum(其它未归档非基础)`；若结果 < 0 不应出现（保存已拦截）。
- 勾选基础且库里已有另一未归档基础：保存时把旧行 `IsBase=false`、`Probability=0`，并 `MessageBox`：**「{旧标题}不再设为基础奖励，抽取概率已设为0%。」**（目标原文「设为非基础奖励」里多了一个「非」字，按此句落地。）
- 抽奖：只从未归档池抽；按百分比抽（建议 0–100 均匀随机落在累计区间；基础奖占剩余）。无基础且合计 < 100：剩余概率 → 见提问（默认：视为未中任何「列出的奖」，或强制要求有基础奖）。
- 卡片文案：`权重 N` 改为 `概率 N%`；基础奖显示重算后的当前值（可标「基础」）。
- 种子数据：改成百分比 + 指定一条基础（或四条显式百分比且一条 IsBase）。旧库已有 Weight 行：启动或首次打开奖励页时，若 `Probability` 全空，可按权重归一化迁移一次——见提问。



### 新增主题 / 库表（相对迭代 5）

- 主题：`tooltip-background` / `tooltip-text`；（建议）`button-*` 悬停对；`week-px-per-hour`；`overflow-track` / `overflow-fill`。
- Noco：`reward_pool.Probability`、`IsBase`；sessions 删除走 API（无新列）。
- `NocoClient.DeleteRecordAsync`。



### 不要纳入 / 改不到的

- 系统 `ColorDialog` / MessageBox 外观。
- 撤销已发的抽奖次数或 completion（删 session 不自动退奖）。
- 翻周、收藏归档、WebP。
- 用权重和百分比两套并存给用户选（本轮只留百分比）。



### 落地时注意（实现轮再做）

- Button / ToolTip 模板与主题同一轮交，否则黑夜悬停仍坏。
- 删 session 前确认文案用缓存字段，避免只显示 Id。
- 换基础奖时两次 Patch（旧行 + 新行）或先列后写，保证只有一个 `IsBase`。
- 同步 `窗口信息.md`：日程删除、奖励编辑字段、设置项名称。



### 提问

1. **页签与按钮是否共用同一对「悬停底/悬停字」参数？**
  **默认：** 分开：`tab-`* 保持；新增 `button-hover-`*。闲置按钮继续表面+主字。视觉上可把黑夜预设调得接近，但设置页两套，避免改按钮误伤页签选中态。
2. **短日程块（高度不够三行字）？**
  **默认：** 左半仍画得下的字（可缩小到下限）；实在叠不下则左半只保留任务名，起止只放 ToolTip。
3. **删除 session 是否回滚发奖 / completion / 溢出？**
  **默认：** 不回滚，只删执行记录。
4. **无基础奖励且未归档固定概率之和 < 100% 时，剩余概率？**
  **指定：** 抽取前弹窗提示无基础奖励；本次抽取将各奖设定概率**视为权重**生成随机结果（不按「空档」处理）。有基础奖时仍按百分比区间抽。
5. **旧数据 Weight 如何变成 Probability？**
  **指定：** 旧权重丢弃；现存所有奖励设为 **0%** 概率（不迁移归一化）。
6. **归档的基础奖是否仍占「唯一基础」名额？**
  **默认：** 否。归档后不算；允许另设一条未归档基础。抽奖池本就不含归档。
7. **确认框里「xxx不再设为基础奖励…」在用户点保存时立即弹出，还是仅写状态栏？**
  **默认：** 保存成功前 `MessageBox` 提示一次（确定即可，不是 Yes/No 确认）。



## 迭代7

- 在日程记录中添加每小时一道横向指示虚线
- 将日程记录中的周二、四、六列背景改为稍微不同的颜色，用来从视觉上区分不同日期。这个颜色可以在设置页设定。
- 在日程记录中添加一道横向的较粗虚线指示当前时间。这个虚线的位置需要即时计算。
- 在日程记录的窗口内右上角新增一个”回到当前时间“的按钮。点击回到当前时间后，程序进入自动跟随，会随时间更新下滑。用户手动控制slide bar或使用滚轮滑动后，取消自动跟随。自动跟随状态中不显示回到当前时间按钮。
- 卡片缩略图新增重新截取缩略图区域功能。
- 重大更新：在数据结构不变的前提下，除收藏夹以外的模块不再依赖nocodb，而是将nocodb作为可选项。
  - 目前的”设置“页更名为”界面“
  - 新增设置页来放置nocodb服务设置等设置内容
  - 收藏夹和其他数据可以单独开启或关闭nocodb服务
  - 关闭nocodb服务时，将noco中的数据下载到本地进行持久化。
  - 开启nocodb服务时，验证noco可以连接、建立schema无误后，将数据上传到noco中，并且删除本地数据。
  - 无论nocodb是否可用，程序都将打开。在noco不可用时，依赖noco服务的功能页面中的所有按钮全部设为不可用状态，页面中仅居中显示”NocoDB未连接，请配置NocoDB或将此页面功能设为不依赖NocoDB后重试“和下方的”尝试连接NocoDB“按钮。
  - 界面 和 设置两个页面永不依赖nocodb，所以不会灰掉
  - 尝试连接NocoDB的具体行为可以在设置页中配置。可以配置为指定链接上的nocodb服务，也可以配置为通过命令行拉起本地的nocodb服务，并访问指定链接（这里的指定链接就是nocodb了）
- 重大更新2：将任务页合并至日程记录页中，使任务成为日志记录页的左侧边栏
  - 删除任务页中的开始执行和测试提醒按钮
  - 删除所有页面中的刷新按钮，改为点击f5刷新。
  - 在设置页中新增”快捷键“部分。将f5刷新的配置加入其中，但是配置按钮灰掉不可用，仅展示。
  - 用户可以在日程记录页面长按鼠标来框选鼠标所在列的日期的一段区域。这段被框选的区域显示为蓝色不透明虚线边框。此时可以右键选中区域，打开右键菜单。右键菜单中有”标记为指定活动“选项，鼠标点击后显示右键菜单次级菜单，将所有可用的任务的名称放入此菜单中。点击任务即可将所选的时段标为指定的活动。
    - 活动记录块的底部和顶部都会吸附用户的鼠标
    - 当前时间的虚线会吸附用户的鼠标
    - 用户所选的区域覆盖已有的记录块，或超过当前时间时，右键菜单中的”标记为指定活动“选项会灰掉，无法选中。



### 可行性评估（不改上方需求原文）

**总体：可行，仍用现有 WPF +（可选）Noco。** 不换框架。本轮实际是三类工作叠在一起：（A）日程交互/视觉；（B）任务页并入日程 + 快捷键；（C）Noco 变为可选项（架构级）。（C）工作量显著大于（A）（B），应单独成步，避免与框选改版抢同一文件长时间冲突。

与文末 Future Feature「无法连接 noco 的 fallback」**目标重叠**；落地本轮重大更新后，那条应视为已覆盖或删改，避免双份描述（本评估不改 Future 原文，只提示）。

#### 现状对照（已查代码）


| 需求点              | 现状                                                                                                      |
| ---------------- | ------------------------------------------------------------------------------------------------------- |
| 每小时横向虚线          | `RenderWeekBoard` 仅每 **4** 小时画实线参考线；无每小时虚线                                                              |
| 二/四/六列不同底色       | 七列共用 `WeekColumnBackgroundBrush`；无隔日色、无主题项                                                              |
| 当前时间粗虚线          | 无；无定时器按「现在」重算 Y                                                                                         |
| 「回到当前时间」+ 自动跟随   | 周板在 `ScrollViewer` 内，无跟随状态、无滚轮/滚动条取消跟随                                                                  |
| 缩略图重新截取          | 编辑窗仅「浏览…」换新文件再裁；**不能**对已有远程/本地 Thumb 再开 `ThumbCropWindow`                                               |
| Noco 可选 / 本地持久化  | `App.OnStartup` 必须 Docker+登录+`SchemaService`，失败则退出；业务全走 `NocoClient`；本地仅有 `styles.json` / `window.json` |
| 设置→界面、新设置页       | 单一「设置」页签 = 主题编辑器                                                                                        |
| 双开关（收藏 / 其它）     | 无                                                                                                       |
| 任务并入日程左侧栏        | 独立「任务」`TabItem` + 独立工具栏（含开始执行、测试提醒、刷新）                                                                  |
| 去掉各页刷新、F5        | 任务刷新=`ReloadAllAsync`；日程/愿望/收藏各有刷新；无 F5                                                                 |
| 快捷键区仅展示          | 无                                                                                                       |
| 长按框选列内时段 + 右键标活动 | 日程块仅单击选中/删除；无拖选、无吸附、无「写入任意起止 session」的专用入口（现有开始执行从「现在」起算）                                               |




#### 分项可行性

**（A）日程视觉与跟随 — 低风险、可做**

- 每小时虚线：在现有 4 小时实线之外加 `h=0..24` 的 `StrokeDashArray` 线即可。
- 二/四/六列色：主题增 `week-alt-column-background`（名称落地时再定）；`d % 2` 或显式周二四六下标刷底。
- 当前时间线：`DispatcherTimer`（如 1s/30s）按 `WeekPxPerHour` 算 Y；跨日仅当天列显示；离开本周可隐藏或仍画在边界——实现时选一种默认即可。
- 自动跟随：给日程 `ScrollViewer` 记 `_weekFollowNow`；跟随则 `ScrollToVerticalOffset` 使当前时间线落在视口中部或上部；监听 `ScrollChanged` / PreviewMouseWheel，用户一手动就清跟随并显示「回到当前时间」。按钮放该页右上（`Canvas`/`Grid` 叠在滚动区上，勿挡列头）。

**（B）缩略图重裁 — 低风险、可做**

- 编辑窗在已有预览时加「重新截取…」：若仅有远程图，先下载到临时文件再 `ThumbCropWindow.Ask`；确定后与「浏览」一样走上传/本地路径。不新开窗类型。

**（C）Noco 可选 — 高风险、可行但必须抽象存储**

- 「数据结构不变」= 逻辑实体与字段语义对齐现表（tasks / sessions / completions / reward_pool / wishlist / app_state / favorites + 附件），不是继续处处 `JsonNode`+Noco API。
- 需要 `IDataStore`（或业务/收藏两套接口）：Noco 实现 + 本地实现（建议工作区 `Personal_Management/local/`：`*.json` + `files/`；Id 用字符串 Guid）。附件本地化是最大坑：关 Noco 下载时要把 Attachment 拉成文件并改引用。
- 开关切换语义按需求：关=下载到本地；开=连上并 Ensure schema 后上传并**删本地**。实现上必须 **上传校验成功后再删本地**，失败则开关不翻转。云端在「关」之后是否清空需求未写——建议默认保留云端备份（评估建议，非改需求）。
- 双开关：业务页（任务/日程/奖励/愿望/钱包）与收藏夹可分别本地/Noco；灰态文案按「该页所依赖的那一路」判断。界面+设置永不灰。
- 启动：先读本地配置 → 开主窗；再按开关尝试连接。去掉「连不上就 Shutdown」。
- 「尝试连接」可配置 URL 或命令行拉起本地 Noco：现有 `DockerBootstrap` / `nocodb-admin.txt` 可迁到设置页；命令行拉起要超时与失败提示，属设置页行为配置，技术可做。

**（D）任务并入日程 + F5 / 快捷键展位 — 中风险、可做**

- 布局：`TabItem`「日程记录」改为左栏任务墙（复用现卡片/记录任务/播放）+ 右栏周板；删独立任务页签。
- 删工具栏「开始执行」「测试提醒」：播放仍在卡片上；测试提醒若无入口则本轮等于下线（需求已删按钮，评估记一笔）。
- 全局 `PreviewKeyDown`：F5 → 按当前页或整包刷新（任务已并入日程后，F5 建议刷新日程相关：任务列表+周板+钱包若需要）；设置页快捷键区只读展示 F5，控件 `IsEnabled=false`。

**（E）长按框选 + 右键标记活动 — 中高风险、可做**

- 按列命中测试：`MouseLeftButtonDown` 长按阈值后进入拖选；蓝虚线框；松手保留选区。
- 吸附：拖拽边缘 Y 对齐附近 session 顶/底与当前时间线（阈值像素，如 6–8px）。
- 右键菜单：「标记为指定活动」→ 子菜单列未归档任务；写入一条 `sessions`（`StartedAt`/`EndedAt`=选区，`Outcome=success` 或进行中策略需实现时定；需求未写 Outcome——建议默认 success、暂停 0，并走现有结算与否也需定：框选补记更像「记录任务」还是只写日志——**建议默认只写 session、不自动发奖**，与「标记活动」语义更近；若要发奖应另问）。
- 覆盖已有块或超过当前时间 → 菜单项禁用：用几何相交 + `end > DateTime.Now` 判断。

「重大 gen」若文档曾出现半截标题，当前迭代 7 正文未定义，本评估视为**不在本轮范围**。

### 建议的分步实现规划（最多三步）

三步按依赖切开：先可独立交付的 UI，再存储架构，最后高交互框选（依赖合并后的日程页）。

#### 第一步 — 日程增强 + 缩略图重裁 + 信息架构壳

**做：**

1. 每小时横向虚线；二/四/六列可主题配置的底色；当前时间粗虚线（定时刷新）。
2. 「回到当前时间」+ 自动跟随 / 手动滚动取消 / 跟随中藏按钮。
3. 各编辑窗「重新截取」缩略图（复用 `ThumbCropWindow`）。
4. 「设置」改名为「界面」；新增「设置」页签（可先放 Noco 相关占位与尝试连接方式的配置壳，哪怕尚不切换存储）。
5. 去掉各页「刷新」按钮；主窗口 F5 刷新；设置页「快捷键」区展示 F5（按钮禁用）。

**验收：** 不连存储重构也能完整演示日程可读性与跟随；主题能改隔日列色；旧图可重裁。

**不做：** 本地库、开关搬家、任务页合并、框选。

#### 第二步 — Noco 可选（重大更新 1）

**做：**

1. 存储抽象；本地 JSON+附件；业务 / 收藏两个「使用 Noco」开关。
2. 关：自 Noco 下载到本地并之后读本地；开：连接+schema → 上传 → 成功后删本地。
3. 启动始终进主窗；依赖 Noco 且未连接的页：控件禁用 + 居中文案 +「尝试连接 NocoDB」（行为读设置：指定 URL 或命令行拉起再访 URL）。
4. 界面、设置两页永不灰；把 Noco/Docker/命令行等从仅 `nocodb-admin.txt` 扩展到设置页可配（文件仍可作默认源）。

**验收：** 业务可完全离线使用；收藏可单独云端或本地；断网杀 Docker 仍能打开程序。

**不做：** 框选标记；可不做任务页合并（若第一步未合并，本步保持页结构，避免与存储大改叠在同一 PR）。

#### 第三步 — 任务并入日程 + 框选标记活动（重大更新 2）

**做：**

1. 去掉独立任务页；日程页左侧任务栏 + 右侧周板；去掉「开始执行」「测试提醒」工具栏按钮（卡片播放/记录保留）。
2. 长按拖选单列时段、蓝虚线框、顶底与当前时间线吸附。
3. 右键「标记为指定活动」→ 任务子菜单；覆盖已有或超过当前时间则禁用；确认后写入 session（结算策略按上面评估建议默认：只记日志不发奖，除非实现前改口）。
4. 同步 `窗口信息.md`（页签结构、设置/界面、框选、Noco 灰态）。

**验收：** 单页完成「看任务 + 看周 + 补记时段」；F5 仍可用。

---

**步骤之间的原则：** 每步可单独合并/演示；第二步是唯一必须动启动与几乎全部 Load/Save 的步骤；第三步改 `RenderWeekBoard` 交互面最大，放在存储稳定之后。若资源极紧，可把第一步的「设置页壳」缩到最小，把完整 Noco 配置 UI 全部放进第二步——仍保持三步上限。

## Future Feature & Brainstorm

- 地图/锚点笔记
- 置信图
- 命令模式 撤回操作
- 手机终端功能
  - 为个人管理工具制作安卓终端
  - 在同一wifi下建立端到端加密通讯
  - 手机终端在连接状态下可开启任务，查看正在执行中的任务状态，收到满足奖励时间的提示
    - 手机终端在本地计时来进行即时渲染，但是每隔数秒都会同步电脑终端状态，以电脑的计时为准。
    - 在手机开启任务后，电脑也会和正常开始任务的流程一样进行弹窗。手机确认退出时电脑也会对应地退出。
  - 手机终端无法添加/归档任务，无法抽奖。未来可能会添加相关功能，但目前但凡没有明确提到的功能，手机都不添加。
  - 手机终端可以查看收藏
    - 手机和电脑端都可以用内置的查看器来查看收藏大图。具体的查看器功能设计待定。
  - 手机终端的数据采取”阅后即焚“的模式。用户可以手动清除缓存，程序启动时也会清除上次的缓存。就是不知道程序关闭时安卓系统会不会给钩子让我们删缓存。
- 实体/事件数据库
  - 用极简的方式记录实体/事件数据
  - 实体数据：名称，地址，电话，缩略图，生日，笔记，别名（多个）...
  - 事件数据：名称，地址，关联实体（多个），关联事件（多个），笔记，别名（多个）...
  - 从周哥那里拿他那边的数据schema（这一条agent不用管，涉及现实信息）
  - AI自动记录
    - 从语音中识别和整理实体数据和事件数据
    - 事件数据中如果提及了关联实体和关联事件，需要推理确定提及的实体和事件是哪个以及是否存在。
    - 如果确认到用户使用了某个实体或事件的别名，可以选择为实体或事件新增别名，以方便日后搜索。agent不采用读取整个数据库表格的方式读数据库，而是采用关键词主被动结合探知+模糊搜索的方法获取指定的数据，防止上传token和推理消耗过大。
    - 在最终录入数据之前，AI向用户提交结构化改动草案。改动草案中按数据库交互顺序，结构化地列出AI所理解的本次用户要求的改动内容。其中包括新增数据、旧数据改动（可能是添加别名，方便AI下一次模糊搜索）。
    - 用户可以在结构化改动草案中直接调整内容。在校验草案合法之后，可以应用到数据库上
  - AI自然语言翻译查询请求
  - 固定频繁使用的查询请求
  - 接入微信/qq
- 提醒/轻对话功能接入微信/qq
- 图片查看器+笔记钉


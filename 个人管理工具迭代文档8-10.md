## 迭代8

- 单独调整日程记录页的任务卡片尺寸
- 在界面页中区分section：每个页的参数调控选项集中在一起。顶部有“通用”section，用来调整全局通用的参数
- 日程记录页中“当前时间”标线应贯穿每一列，而不是仅表示当前日期。在当前日期所在列的竖中线与当前时间标线的相交之处新增一个小圆圈，代表定位到今天。
- 查看当前设置卡片缩略图时是否不保留原图。如果不保留，改为保留原图，并且使重新设置缩略图时可以使用原图来设置。重新设置缩略图时，以上一次设置的缩放、位置开始。
- 目前的任务卡片在设置缩略图后四周不再是圆角，查看此表现是否符合预期，如果符合代码预期，改为设为圆角。
- 使日程记录页的任务边栏可以拖动来放大/缩小。拖动松手时，自动收缩边栏宽度来适配当前已展示的卡片列数。即是说：如果用户拖动边栏使边栏展示2列卡片，则收缩边栏使其正好展示2列卡片。

## 迭代9

- 剩余抽奖次数不在程序下边栏显示。
- 使个人管理工具可以配置LLM API KEY，以供未来进行LLM功能开发。注意这类信息在设置页中都提供隐藏/显示按钮。隐藏状态下所有字符显示为黑色圆形符号。
- 将奖励和愿望单合并为一个页面，分为上部和下部。剩余抽奖次数和剩余额度仅显示在日程记录页和奖励/愿望单子页中。
- 新增体重记录功能模块。用户可以输入自己的身高、年龄、运动情况，程序会自行计算BMI、基础代谢等实用信息。体重记录单独成页，单独开闭nocodb依赖，部分样式单独设置。
  - 目前体重的批量输入格式如下：  
  yyyy-mm-dd \t 11.11  
  a \t 11.11  
  a \t 22.22  
  yyyy-mm-dd \t 33.11  
  yyyy-mm-dd \t 44.11  
  a \t 11.11  
  tab分割日期和体重（kg），用户只能输入小数点后2位的体重。  
  如果日期一栏写的是a，代表这一天是上面那天的下一天  
  如果日期一览写的不是a也不是合法日期，拒绝此次输入，弹出错误提示。
  - 体重也可以选择计当天体重
- 奖励/愿望单将作为日程记录页的弹窗式子页。日程记录页提供打开奖励/愿望单窗口的按钮。

## 迭代10

- 记录状态下不可用框选方式记录任务，右键菜单相关选项灰掉
- 点任务执行窗 ×：收起窗口（不结算）；日程顶栏右上「当前执行：xxxxx」带进度条，点击唤回执行窗
- 计时/暂停状态依赖主窗口；失败/退出仍须打开执行窗操作
- 体重折线纵轴：历史最高+2 向上取整 ～ 历史最低−2 向下取整；每 1kg 一条横向虚线
- 缩略图选区窗：高 DPI（如 300）图在缩放移动阶段只见灰底——按像素设控件尺寸 + Stretch=None 时内容只画在一角，视口对准空白；导出仍正常。已规范为 96 DPI 并 Fill
- 界面页「每日仅显示一部分时间」：起始/结束整点 0–24（默认 6–23，显示到 23:00；0–24=全日）；跨隐藏区色块只画可见部分；新写入若起止在未显示小时则提示，可不再提示
- 任务「日程记录块样式」：右/左斜纹、棱形/星/圆点/月亮散布 + 样式色；底色改名为日程记录块颜色；纹样不透明叠底；存 BlockPattern / BlockPatternColor；周板与无缩略图卡片展示；编辑窗有预览
- **日程记录块样式编辑窗（所见即所得）：** 多层叠加（增删/上下移）；统一参数：粗细、间隔、角度、透明度、图案尺寸、X/Y 重复偏移；新增正弦纹路（粗细/间隔/角度，尺寸=振幅）；HLS 色环+三角取色，先选底色或层颜色再点/拖即时预览；存 `BlockStyleJson`（旧字段兼容迁移）；任务窗「高级编辑…」接入；`Probes --block-style-editor` 可单独运行；**预设**（应用 / 存为预设同名覆盖 / 删除，文件 `block-style-presets.json`）
- 新建/编辑任务窗：缩略图可「取消缩略图」，保存时清空 Thumb、Original、CropJson
- 周板：当前时间横虚线默认更细、约 50% 透明，界面可调颜色/粗细/透明度；色块整列宽；去掉块内起止时间字；任务名居中+黑描边；悬浮提示改「x月x日」并加总计时长（仅呈现）


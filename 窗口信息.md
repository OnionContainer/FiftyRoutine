# 窗口信息

改窗口（增删、改名、改内部控件、改跳转/弹出关系）时，必须同步改这份文档。`claude.md` 里也写了这条。
下列「名称」是文档用名；「标题」是窗口 Title 栏上的字。

---

## 启动准备窗

- **标题：** 个人管理
- **类：** `App` 里临时创建的 `Window`（无独立 xaml）
- **内部：** 一行状态文字，先显示「正在启动…」；选定用户后显示「正在加载用户…」；若该用户设置里业务、收藏或体重启用了 Noco，再尝试连接并更新状态字
- **跳转：** 先走登录（或直接登录跳过）→ 加载用户 → 关掉自己并打开 **主窗口**（依赖云端且未连接的页在主窗口内灰显）。致命错误时弹出「启动失败」并退出；用户取消登录则安静退出
- **图标：** 若 exe 旁有 `Assets/app.ico` 则窗口用它；否则系统默认

---

## 登录窗

- **标题：** 个人管理 — 登录
- **类：** `LoginWindow`
- **内部**
  - **无用户：** 仅「新增用户」：用户名；业务/收藏/体重三个 Noco 勾选（默认全关）；至少勾一项才可编辑 URL / Email / Password / ApiToken；「创建并进入」
  - **有用户且未直接登录：** 用户列表（双击或「进入所选用户」）+ 下方「新增用户」同上
  - **检测到旧数据且尚无用户：** 提示将迁移；仅「迁移并新建用户」（用户名 + 可选 Noco）；「迁移并进入」
- **从哪来：** 启动时；若 `ProgramData/app.json` 中「直接登录」开且上次用户目录仍在，则跳过本窗
- **跳转：** 选定/创建成功 → 写上次用户 → 启动流继续打开主窗口。取消 → 退出进程
- **无密码；暂无登出 / 删用户**

---

## 主窗口

- **标题：** 个人管理
- **类：** `MainWindow`（壳；实现 `IAppHost`）
- **源码位置（迭代14）：** `Desktop/Window/MainWindow.*`；六大页签 UI/逻辑在 `Desktop/Page/`（`SchedulePage` / `FavoritesPage` / `WeightPage` / `GadgetsPage` / `AppearancePage` / `SettingsPage`）；基础设施在 `Desktop/Services/`；启动壳在 `Desktop/App/`；`HlsColorPicker` 在 `Desktop/Control/HlsColorPicker/`。命名空间仍为 `PersonalManagement.Desktop`。
- **内部**
  - 底栏：仅右侧状态字（抽奖次数 / 愿望单额度不再放底栏）
  - 六个页签（各页顶部功能栏可换行，无溢出滑动块）。主窗口 **F5** 刷新全部业务数据（`ReloadAllAsync`）；日程页 **F1** 切换聚焦模式（用聚焦每小时高度并跳到当前时间）；各页无「刷新」按钮，工具栏有「F5 刷新」提示字
    - **日程记录：**（任务已并入本页）顶栏左侧「新建任务」「删除」「奖励 / 愿望单…」；勾选「显示已归档内容」；钱包字可点击 → **调整钱包**（当前奖券数量 / 愿望单额度，≥0 整数，便于从其他系统迁入）；提示「拖入图片或 Ctrl+V 新建 · 长按列内拖选补记 · 右键笔记钉 · F1 聚焦 · F5 刷新」。顶栏**右上**「当前执行：任务名」带圈内进度条按钮（有进行中任务时显示；暂停时后缀「（已暂停）」；点击 → 打开/唤回 **任务执行窗**）。**左侧**任务栏（可拖分隔条改宽，松手后按当前列数吸附并持久化列数；宽度按列数与任务卡宽现场计算，刷新/改主题卡宽时重算；宽度始终预留竖向滚动条以免有/无滚动条时错列或裁切；任务卡宽/高/图区高用日程专用主题参数）：任务卡片墙（圆角裁切缩略图或颜色块、名称、类型、奖励等级、阶段/状态；缩略图右上红色菱形「记录任务」+「开始任务」播放钮；允许溢出时左侧溢条）。拖入/Ctrl+V → **新建任务窗**。双击卡片 → 编辑。播放 → **任务执行窗**。菱形「记录任务」写一条成功执行并结算。「开始执行」「测试提醒」工具栏按钮已去掉。**右侧**上为周板、下为可收起「日程块统计」（默认收起；点标题展开；近 7/14/30 天切换；折线：直接生产力 / 非直接生产力，纵轴 0–16h；展开高度与天数写入 `window.json`）。周板：当前自然周一到周日七列（每小时高度分常态 `WeekPxPerHour` / 聚焦 `WeekFocusPxPerHour`；**F1** 切换聚焦并跳到当前时间，再按退出）。每日纵轴仅画界面页设定的可见整点窗 `[WeekHourStart, WeekHourEnd]`（默认 6–23，底边 23:00；0–24 即全日）；色块与隐藏时段相交时只画可见部分。每小时横向虚线；起止与每 4 小时实线。二/四/六列「二四六列底」。**七列**均画「当前时间」横虚线（颜色/粗细/透明度可在界面页调，默认约 50% 透明、较细；当前时刻若在未显示小时内则隐藏）；今天列竖中线与时间线交点有小圆定位点；右上「回到当前时间」自动跟随（滚轮/拖条取消，跟随中藏按钮）。鼠标在周板列上时：Accent 色横向悬停虚线 + 左侧时间标签显示光标时刻（与「当前时间」线区分）。色块整列宽（底色+不透明纹样；失败态整体降透明）；任务名居中并黑描边（无起止时间字）。悬浮提示为「任务名 x月x日 HH:mm–HH:mm · 共时长」（跨日则两端都带月日；无年份）。点选色块 →「删除」确认删 session（不退奖）。**长按**某一列空白约 0.4s 后拖选时段：蓝色不透明虚线框；边缘吸附已有块顶/底与当前时间线（**有任务正在执行时不可框选补记**）。**右键**周板列（落点）：「添加笔记钉」→ **笔记钉编辑窗**；若当前日有有效拖选，菜单另含「标记为指定活动」→ 未归档任务子菜单（覆盖已有记录、结束晚于现在、或有任务正在执行则该项禁用；确认后只写 session、success、不发奖）。**笔记钉**：坐标 = `At`（日期时间）+ `DayColumnPercent`（列内 0–1）；Accent 小圆点；仅可见小时内显示；左键点钉展开正文 Popup（点别处收起）；双击 → 编辑窗；编辑窗可删。存业务表 `schedule_notes`，随 `UseNocoBusiness` 本地/云同步。**新写入** session（框选补记 / 记录任务 / 开始执行 / 结束执行）若开始或结束落在未显示小时内 → 弹窗提示可到【界面】调整；可勾「不再提示」（写入 `window.json`）
    - **收藏夹：** 「添加」「复制图片」「HoneyView」「导出所选」；标签下拉（全部 / untagged / 各 tag）；勾选「显示私密 tag」；提示「拖入图片或 Ctrl+V · F5 刷新」；卡片墙（勾选框 + 缩略图 + 名称一行；宽/高/图区高单独进主题，宽度固定像素，不随窗口比例变）
    - **体重：** 顶栏「记今天」「批量粘贴…」「保存档案」；档案区身高 / 年龄 / 性别 / 活动水平；统计字（最近体重 · BMI · BMR · TDEE）；横向范围（起始日/结束日，默认今日−30～今日，「近30天」「应用」）；**折线图**（纵轴：全部历史最低体重−2kg 向下取整 ～ 历史最高体重+2kg 向上取整；每 1kg 一条横向虚线；样式在界面页）。「记今天」→ 文本提示输入 kg；「批量粘贴…」→ 多行窗（`AcceptsTab`；日期与体重用空格或 Tab 分隔；`a`=上一日+1）。独立 Noco 开关 `UseNocoWeight`
    - **小玩意：** 杂项小功能页（不依赖 Noco，永不灰显）。当前仅 **镜像文字**：同一矮行——文本框 → 即时预览小图（白底；第一步图左右留白默认各 1px，可由同行「左留白」「右留白」输入框改；左侧「？！」+ 输入文字，右侧为其水平镜像）→「复制」→ 左/右留白像素；底栏状态字提示已复制
    - **界面：**（原「设置」主题编辑）当前样式下拉（普通模式 / 黑夜模式 / 用户副本）；「另存为副本」「删除副本」。参数按 section：**通用**（全局色/字号/卡片圆角、**右键菜单底/字/悬停底/悬停字**、悬浮提示、按钮、选项栏等）→ **日程记录**（周列色、**当前时间线颜色/粗细/透明度**、任务卡宽高/图区高、每小时高度常态 8–150 / 聚焦 8–200 默认 100、**每日显示起始时/结束时** 0–24 默认 6–23、开始按钮等）→ **奖励 / 愿望单**（卡片宽高/图区高）→ **收藏夹**（收藏卡宽高/图区高）→ **体重**（折线颜色/网格/图区背景、折线粗细、数据点大小、图区高度）。本页永不因 Noco 灰显
    - **设置：** **程序**区：当前用户名提示；「直接登录上次用户」（写入 `ProgramData/app.json`，默认开；关掉后下次启动进登录窗）。**NocoDB** 区：业务 / 收藏 / 体重三个「使用 NocoDB」开关；服务地址 / Email / Password / ApiToken / Docker 容器名 / HoneyView（均读写当前用户 `settings.json`，缺项即未配置，不再回退 txt）；Password、ApiToken 带「显示/隐藏」（隐藏时用 ● 遮罩）。**LLM** 区：ApiKey（同显隐）、BaseUrl、Model。「尝试连接方式」下拉（仅访问 URL / 命令行拉起 Docker 后再访 URL）；「保存连接设置」「尝试连接 NocoDB」「应用存储开关（会迁移数据）」。关开关：已连接则下载到 `UserData/<用户>/local/{business|favorites|weight}/` 再切本地；未连接则直接切本地。开开关：连接+schema → 上传 → 成功后清空对应本地库。快捷键区：「刷新数据」旁灰色「F5」；「日程聚焦」旁灰色「F1」（仅展示）。本页永不因 Noco 灰显
  - **Noco 灰显：** 当对应开关为「使用 Noco」且当前未连接时，日程（含左侧任务，业务）、收藏夹或体重页：内容半透明禁用，居中文案「NocoDB未连接…」+「尝试连接 NocoDB」
  - **关闭：** 藏到托盘，不退出。宽高、位置、最大化，以及日程左侧任务栏**列数**、**不再提示未显示时间**写入当前用户 `UserData/<用户>/window.json`，下次启动还原
  - **可弹出 / 跳到**
  - 日程页「新建任务」、左侧任务栏拖入/粘贴图片 → **新建任务窗**（选图后先开 **缩略图选区窗**）
  - 日程页双击任务卡 → **新建任务窗**（编辑任务）
  - 日程页双击周板色块 → **新建任务窗**（编辑关联任务）
  - 日程页任务卡播放键 → **任务执行窗**
  - 日程页顶栏「当前执行：…」→ 打开/唤回 **任务执行窗**
  - 日程页卡片菱形「记录任务」→ **记录时长窗**（10/20/30 分、1 小时、至今）后写成功 session 并结算；若已有进行中任务则唤回执行窗并提示
  - 日程页「奖励 / 愿望单…」→ **奖励 / 愿望单窗**（已打开则 Activate）
  - 到点提醒 → **任务提醒窗**（工具栏「测试提醒」已移除）
  - 任务执行满该任务的奖励要求时长 → **任务提醒窗**（多一个「退出」；由主窗口计时触发）
  - 日程页「删除」→ 确认 MessageBox
  - 日程页框选右键「标记为指定活动」→ 写入 session（不发奖）；非法选区或有任务正在执行时菜单项禁用
  - 日程页周板右键「添加笔记钉」→ **笔记钉编辑窗**；左键点钉展开 Popup；双击钉 → 编辑（可删）
  - 新写入 session 且起止落在未显示小时 → 提示 MessageBox 式窗（可「不再提示」）
  - 体重页「记今天」→ 文本提示；「批量粘贴…」→ 多行输入窗；解析失败 → MessageBox
  - 各编辑窗选缩略图 / 「重新截取…」（及收藏选图片文件）→ **缩略图选区窗**，确定后才作为缩略图
  - 界面页取色 → 系统取色板；换开始任务图标 → 系统文件选择器
  - 收藏夹「添加」、拖入图片、Ctrl+V（含粘贴链接）→ **收藏编辑窗**（添加收藏）
  - 收藏夹双击缩略图 → **收藏编辑窗**（编辑收藏）
  - 收藏夹「HoneyView」→ 外部 **HoneyView**
  - 收藏夹「导出所选」→ 系统文件夹选择器
  - 勾选「显示私密 tag」且未设密码 → **二级密码窗**（设置）；已设密码 → **二级密码窗**（输入）
  - 设置页迁移失败 / 连接失败 → MessageBox
  - 各种失败 / 校验 → 系统 MessageBox
- **托盘：** 双击或菜单「打开主窗口」回到本窗；菜单「退出」才结束进程
- **本地配置文件（相对 exe 目录）：** `ProgramData/app.json`（直接登录、上次用户）；`UserData/<用户>/settings.json`（Noco 开关、连接、LLM）；`UserData/<用户>/local/business|favorites|weight/`（JSON 表 + 业务/收藏的 `files/`）；另有同用户下 `styles.json` / `window.json` / `block-style-presets.json` / `assets/`

---

## 新建任务窗

- **标题：** 新建任务 / 编辑任务
- **类：** `NewTaskWindow`
- **内部：** 名称；类型下拉（仅「每日重复」「灵活重复」）；奖励等级 L0–L5（每日任务锁成 L1；L0 无奖励）；**样式预览**条 +「高级编辑…」→ **日程记录块样式编辑窗**（块色/纹样仅在高级编辑中改）；缩略图区两行：上行预览（固定高 64px）+「浏览…」，下行「取消缩略图」「重新截取…」（浏览或重裁后进 **缩略图选区窗**；取消后保存清空 Thumb/Original/CropJson）；奖励要求时长（分钟，1–1440，默认 30）；「直接生产力任务」（悬浮说明用途）；「允许溢出进度」；「已归档」；取消 / 创建或保存。截止/提醒 UI 已隐藏（字段与逻辑仍保留）。改时长或取消允许溢出且当前溢出非零时，确认「改动奖励要求时长将会使溢出进度清零，请问是否确定？」
- **从哪来：** 主窗口 · 日程记录页左侧任务栏（新建按钮、拖入图片、Ctrl+V、双击卡片）；周板色块双击（编辑关联任务）
- **再弹出：** **日程记录块样式编辑窗**、系统文件选择器、**缩略图选区窗**；名称或时长不合法、以及上述清零确认时的 MessageBox
- **样式存储：** 完整样式写 `BlockStyleJson`（底色 + 多层）；旧字段 `BlockPattern` / `BlockPatternColor` 仍由高级编辑回写以便兼容

---

## 日程记录块样式编辑窗

- **标题：** 日程记录块样式
- **类：** `BlockStyleEditorWindow`
- **内部**
  - **预览：** 所见即所得色块（底色 + 各层叠加）
  - **预设：** 伪下拉（Popup）；内置 `ProgramData/DefaultBlockStyle/`（只读）与用户 `UserData/<用户>/BlockStyle/`；一层子文件夹=可收起分组；叶节点点选即载入；「应用」「存为预设」（用户目录，同名确认覆盖）「删除预设」（仅用户）；旧 `block-style-presets.json` 首次打开时迁出为单文件
  - **样式层：** 列表；「添加层」「删除层」「上移」「下移」
  - **统一参数（每层）：** 类型（含**纯色** / 斜纹 / 正弦 / 散布类）、「混合」（普通 / 正片叠底 / 叠加；暂无柔光）与各数值参数均为「标签 | 控件」同行紧凑布局；按 Kind **隐藏**无效项（纯色仅透明度+混合；斜纹无图案尺寸；棱形/星/圆点/月亮无粗细；正弦全开）；旁路可手输；切层同步。非「普通」混合在绘制时像素合成，不烘焙样式参数
  - **取色：** 「底色」「层颜色」切换目标；HLS 色环 + 三角形取色盘；点击/拖动即时改预览；当前色色块
- **从哪来：** **新建任务窗**「高级编辑…」；也可独立运行 `Probes --block-style-editor`（`RunStandalone`）
- **再弹出：** 无层时点「层颜色」的提示；存预设名称的 **TextPrompt**；删除预设确认 MessageBox
- **特点：** 纹样本体不透明，透明度作用在整层；确定返回 `BlockStyleSpec`（序列化为 `BlockStyleJson`）

---

## 记录时长窗

- **标题：** 记录日程
- **类：** `RecordDurationWindow`
- **内部：** 说明字；按钮「10 分钟」「20 分钟」「30 分钟」「1 小时」「至今」；「取消」。固定时长若会使结束超过现在则灰掉。「至今」= 从上次任务结束记到当前时刻。
- **从哪来：** 日程页任务卡菱形「记录任务」
- **再弹出：** 无（无上次结束等由日程页 MessageBox）

---

## 笔记钉编辑窗

- **标题：** 新建笔记钉 / 编辑笔记钉
- **类：** `NotePinEditWindow`
- **内部：** 只读一行（落点时间 `yyyy-MM-dd HH:mm:ss` + 列内百分比）；多行「笔记」正文；编辑态多「删除」；取消 / 保存（正文必填）
- **从哪来：** 日程周板右键「添加笔记钉」；双击已有钉
- **再弹出：** 空正文提示；删除确认 MessageBox
- **数据：** 业务表 `schedule_notes`（`At` / `DayColumnPercent` 0–1 / `Body` / `RecordedAt` 入库时间；勿用 Noco 系统列名 `CreatedAt`）；随业务 Noco 开关迁移

---

## 任务执行窗

- **标题：** 任务执行
- **类：** `TaskRunWindow`（状态与计时在主窗口 `TaskRunState` + `_runTimer`）
- **内部：** 任务名；倒计时/已满 N 分钟/暂停状态（N 为该任务奖励要求时长）；进度条（按该任务要求时长循环：第一圈强调色，之后奇数圈金、偶数圈红）；底栏一行右对齐「暂停/继续」「失败」「退出」
- **关闭键（×）：** 收起本窗（Hide），**不**结束任务；计时继续；日程顶栏「当前执行」可再打开
- **失败 / 退出：** 须在本窗操作（或满时长提醒的「退出」）；成功/失败结算后真正关闭本窗
- **从哪来：** 主窗口 · 日程记录页（卡片播放键；顶栏「当前执行」）
- **再弹出：** 满 N 分钟时 **任务提醒窗**（「知道了」「退出」；只在第一圈跑满弹一次；由主窗口计时触发）
- **特点：** 不同时开两个执行；不置顶；主窗口藏托盘时计时仍由主窗口维持。托盘「退出」程序时，进行中的执行按失败收尾。失败不发奖、不加溢出。有进行中任务时不可再「开始/记录」其他任务（唤回本窗并提示），也不可框选补记。

---

## 奖励 / 愿望单窗

- **标题：** 奖励 / 愿望单
- **类：** `RewardWishWindow`
- **内部**
  - 顶栏：钱包（当前奖券数量 / 愿望单额度，点击 → **调整钱包**）、「F5 刷新」提示、状态字；**F5** → `ReloadAsync`
  - 上下分栏（中间可拖分隔条）：**上**奖励（「添加」「抽一次」、已归档勾选、抽奖结果字、奖励卡片墙）；**下**愿望单（「添加」「兑换所选」、已归档勾选、愿望卡片墙）
  - 业务未就绪时内容半透明禁用 + Noco 未连接遮罩 +「尝试连接 NocoDB」
  - 钱包变更后触发 `WalletChanged`（供主窗口同步日程顶栏钱包）
- **从哪来：** 主窗口 · 日程记录页「奖励 / 愿望单…」（Owner=主窗口；已打开则 Activate）
- **可弹出 / 跳到**
  - 「添加」/ 双击奖励卡 → **奖励编辑窗**
  - 「添加」/ 双击愿望卡 → **愿望编辑窗**
  - 「抽一次」在无基础且概率和&lt;100% 时 → 提示 MessageBox
  - 各编辑窗选缩略图 → **缩略图选区窗**
- **特点：** `ShowInTaskbar=True`；居中于 Owner；任务栏可见

---

## 愿望编辑窗

- **标题：** 添加愿望 / 编辑愿望
- **类：** `WishEditWindow`
- **内部：** 名称；所需额度；缩略图「浏览…」/「重新截取…」/预览（浏览或重裁后进 **缩略图选区窗**）；「已归档」；取消 / 添加或保存
- **从哪来：** **奖励 / 愿望单窗**（添加按钮、双击卡片）
- **再弹出：** 系统文件选择器、**缩略图选区窗**；名称或额度不合法时的 MessageBox

---

## 奖励编辑窗

- **标题：** 添加奖励 / 编辑奖励
- **类：** `RewardEditWindow`
- **内部：** 名称；类型（实物 / 愿望单额度 / 奖券）；**额度**（仅愿望单额度类型可填）；**奖券数量**（仅奖券类型可填，≥1）；概率 %（0–100；基础奖禁用）；「基础奖励」；缩略图「浏览…」/「重新截取…」/预览（浏览或重裁后进 **缩略图选区窗**）；「已归档」；取消 / 添加或保存。未归档非基础概率合计不得超 100%；另设基础时旧基础变非基础且概率 0%，并 MessageBox 提示
- **从哪来：** **奖励 / 愿望单窗**（添加 / 双击）
- **再弹出：** 系统文件选择器、**缩略图选区窗**；名称/额度/概率不合法、概率合计超限、换基础奖提示时的 MessageBox

---

## 收藏编辑窗

- **标题：** 添加收藏（新建）/ 编辑收藏（二次修改）
- **类：** `FavoriteAddWindow`
- **内部**
  - 顶部预览：有图则显示缩略图；没有则按类型显示占位字「图片 / 链接 / 视频」
  - 类型（图片 / 链接/文字 / 视频）
  - 名称、来源、标签（逗号分隔，`*` 开头为私密 tag）
  - 「整条标为私密」
  - 文件浏览（图片/视频）；若是图片，随后开 **缩略图选区窗** 生成 Thumb（主文件仍是原图）
  - 缩略图「浏览…」/「重新截取…」（链接/视频可选；图片也可另选或对已有预览重裁；随后开 **缩略图选区窗**）
  - 取消；添加 或 保存
- **预览规则：** 优先刚选的缩略图文件，其次刚选的主文件（若是图片），再其次编辑时已有的远程预览；视频/链接没有图时用占位字
- **从哪来：** 主窗口收藏夹的添加按钮、拖入、Ctrl+V、双击卡片
- **再弹出：** 系统文件选择器、**缩略图选区窗**；名称/缺文件时的 MessageBox

---

## 缩略图选区窗

- **标题：** 选择缩略图区域
- **类：** `ThumbCropWindow`
- **内部：** 固定选区框（长宽比按该页卡片图区）；其下为原图。滚轮缩放、拖动平移图片；选区框不动。「确定」裁出 PNG 临时文件并记下缩放/平移（`CropJson`）；「取消」等于没选这张缩略图。业务侧另存 **Original** 附件（原图副本），「重新截取…」优先用原图并以上次 `CropJson` 恢复缩放/位置。读图在打开本窗之前完成；载入后规范为 96 DPI（避免高 DPI 图在选区里只见灰底）；不支持 WebP。无原图时可用本地缩略图或内存预览再开本窗
- **从哪来：** 新建/编辑任务、奖励、愿望、收藏里选缩略图或「重新截取…」后；任务页拖入/Ctrl+V 预填缩略图后；收藏选图片主文件后
- **再弹出：** 读图失败或 WebP 时的 MessageBox

---

## 二级密码窗

- **标题：** 设置二级密码 / 二级密码（调用时传入）
- **类：** `PinWindow`（代码拼出来的 Window）
- **内部：** 说明字；密码框；设置时再多一个「再输一次」；确定
- **从哪来：** 主窗口勾选「显示私密 tag」
- **再弹出：** 两次不一致、少于 4 位时的 MessageBox

---

## 任务提醒窗

- **标题：** 任务提醒
- **类：** `ReminderWindow`（代码拼出来的 Window）
- **内部：** 任务名 + 说明；「知道了」；满该任务奖励要求时长时多一个「退出」（成功结束进行中的执行，不依赖执行窗是否打开）
- **从哪来：** 主窗口到点提醒、主窗口计时第一圈跑满
- **特点：** 置顶；弹出时 `Console.Beep`

---

## 系统托盘

- **提示字：** 个人管理
- **菜单：** 打开主窗口；退出
- **双击：** 打开主窗口

---

## 外部 / 系统对话框

| 名称 | 何时出现 | 说明 |
|------|----------|------|
| HoneyView | 收藏夹点 HoneyView | 外部看图程序，路径在 `nocodb-admin.txt` |
| 系统文件选择器 | 收藏编辑窗浏览文件/缩略图；新建任务窗选缩略图；愿望/奖励编辑窗选缩略图；界面页换开始任务图标 | `OpenFileDialog`；选中图片后还会再开 **缩略图选区窗**（界面页换图标除外） |
| 系统取色板 | 日程块样式编辑窗（底色/层色也可走 HLS）；界面页改主题颜色 | `ColorDialog` / HLS |
| 系统文件夹选择器 | 导出所选 | `FolderBrowserDialog`，说明「导出到文件夹」 |
| 系统 MessageBox | 校验失败、同步失败、启动失败等 | 标题随场景，如「启动失败」「同步失败」 |

探针项目 `Personal_Management/Probes` 里还有独立的实验窗，不算正式产品窗口。例：`--block-style-editor` 单独打开 **日程记录块样式编辑窗**；`--thumb-pixels` 缩略图像素探针。

---

## 波形测试窗（playground）

独立测试程序，不是个人管理的一部分。`dotnet run --project playground`

- **标题：** 波形测试
- **类：** `Playground.MainWindow`
- **内部**
  - 波形列表：每行左侧「−」删除；波形下拉（正弦 / 方波 / 锯齿 / 三角 / 白噪声 / 粉红噪声）；频率 Hz；音量 0–1
  - 列表最下方「+」：再加一路波形，混音播放
  - 时长（秒）；播放；停止
  - 底栏状态字
- **持久化：** `playground/last-settings.json`，改参数、加减波形、关闭窗口时写入；下次启动自动载入
- **再弹出：** 时长非法或播放失败时的 MessageBox

---

## 图片加密验证窗（playground/ImageCrypto）

独立验证程序，试 PBKDF2 + AES-GCM 加解密图片；不是个人管理的一部分。  
`dotnet run --project playground/ImageCrypto`

- **标题：** 图片加密验证
- **类：** `Playground.ImageCrypto.MainWindow`
- **内部**
  - 密码框；「选图加密…」→ 预览原图并保存 `.pmimg`；「打开密文解密…」→ 右侧预览；「导出解密图…」
  - 底栏状态字
- **算法：** PBKDF2-SHA256（200k）派生 32 字节密钥；AES-GCM；文件头 Magic `PMIMG1` + salt/nonce/tag + 密文
- **再弹出：** 密码空、解密失败（错密码/篡改）、读写失败时的 MessageBox

---

## 数位板研究窗（Painting_API_Study）

独立研究程序。`dotnet run --project Painting_API_Study`

- **标题：** 数位板研究
- **类：** `PaintingApiStudy.MainWindow`
- **内部**
  - 左侧 `InkCanvas`：把手写笔/鼠标轨迹画上去，压感影响线宽；橡皮端用暗红
  - 右侧「即时数位板数据」：设备名、能力、接触/悬停、X/Y、压感、倾斜/方位/扭转、按键；无板时标明来源是鼠标
- **输入：** WPF Stylus（Windows Ink / 数字化仪栈），不另装 Wacom SDK。需本机已装 Wacom 驱动才会出现 `TabletDevice`

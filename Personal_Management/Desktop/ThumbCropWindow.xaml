<Window x:Class="PersonalManagement.Desktop.ThumbCropWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择缩略图区域"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight">
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="取消" IsCancel="True" Click="Cancel_Click" Margin="0,0,8,0" VerticalAlignment="Center"/>
            <Button Content="确定" IsDefault="True" Click="Ok_Click" Margin="0" VerticalAlignment="Center"/>
        </StackPanel>
        <TextBlock DockPanel.Dock="Top" Margin="0,0,0,12" TextWrapping="Wrap"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   Text="滚轮缩放，拖动移动图片。中间选区框不动。"/>
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
            <Canvas x:Name="Viewport"
                    ClipToBounds="True"
                    Background="{DynamicResource SurfaceBackgroundBrush}"
                    Cursor="SizeAll"
                    MouseWheel="Viewport_MouseWheel"
                    MouseLeftButtonDown="Viewport_MouseLeftButtonDown"
                    MouseLeftButtonUp="Viewport_MouseLeftButtonUp"
                    MouseMove="Viewport_MouseMove">
                <Image x:Name="Photo" Stretch="Fill" RenderTransformOrigin="0,0"/>
            </Canvas>
            <Border x:Name="Frame"
                    BorderBrush="{DynamicResource AccentBrush}"
                    BorderThickness="2"
                    Background="Transparent"
                    IsHitTestVisible="False"/>
        </Grid>
    </DockPanel>
</Window>

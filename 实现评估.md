# 个人管理系统：实现评估

本文评估 `个人管理工具需求.md` 的实现难度、语言选型，以及实现前必须先跑通的功能。  
**程序主体已确定为 C#（WPF）。**

开工前探针已闭环。原型已按默认选项开工（关闭藏托盘、基座 `PersonalManagement`、第一刀不含收藏夹、中文、连不上则尝试 `docker start`）。

---

## 总体难度

这是一个 **中等** 的 Windows 桌面应用：业务规则不难。提醒改为自己的置顶弹窗后，不再依赖 Windows 通知接口。剩下的重量在 GUI、NocoDB 附件、收藏夹查看器。

| 模块 | 难度 | 原因 |
|------|------|------|
| NocoDB CRUD（任务/奖励/愿望单） | 低 | REST + token，标准增删改查 |
| 任务类型与奖励概率 | 低 | 纯规则 |
| 执行记录（点一下开始/结束） | 低 | 写两条时间戳 |
| 愿望单额度 | 低 | 计数与扣减 |
| 提醒 | 低–中 | **托盘常驻 + 置顶弹窗**，不用 Toast |
| 周日程表 | 中 | 日历 UI |
| 图片收藏 + 缩略图查看器 | 中高 | 附件、缩略图、批量、剪贴板 |
| HoneyView、私密 tag | 低–中 | HoneyView 已打通；私密 tag 后置 |
| AI 抓 X 点赞（未来） | 高 | 先不做 |

**MVP（任务 + 抽奖 + 愿望单 + 执行记录 + 弹窗提醒）**：原型可以很快搓出来。  
**含图片查看器的完整版**：更久，查看器仍会占不少时间。

---

## 已拍板的决策

| 议题 | 结论 |
|------|------|
| 程序语言 | **C# / WPF** |
| 「成功执行一周 / 两周」 | **连续 7 天，允许漏 1 天**。漏 2 天则重新开始计数。两周同理 |
| 完成日期怎么存 | **全部用子表**，不用字段 |
| 图片存在哪 | **只存在 NocoDB 附件**。本机不持久化。打开 / 复制 / 缩略图时临时下载 |
| 程序是否常驻 | **托盘常驻**。真正退出后不要求再提醒 |
| 提醒怎么出现 | **不用 Windows 通知 API**。到点直接弹出 **置顶窗口**（WPF 探针已成功） |
| 剪贴板 | **人眼确认通过** |
| HoneyView | **能打开**。32×32 过小会显示异常；**200×200 多色图正常**。路径：`D:\tools\Honeyview\Honeyview.exe` |
| NocoDB 没开时 | **先尝试命令行拉起现有容器** `docker start nocodb-vibecoding`；若引擎未开则尝试启动 Docker Desktop；仍失败再弹窗。不装假数据 |

---

## 提醒：不用 Toast，改弹窗

Windows Toast 探针里 API 调用成功，但本机通知中心实际上不可用（人当时不在 / 系统未展示）。产品决定：

- **不再使用** `ToastNotification` / `ScheduledToastNotification` / 通知中心。
- 到点由托盘里的程序弹出 **WPF 置顶窗口**（`Topmost`，并尽量抢前台），用户点掉或看完关掉。
- 关机或把进程彻底退出后，不提醒。这是接受的。

调研中的「预约 Toast / 任务计划程序」仅作记录，主程序不走这两条。

---

## 语言：已选定 C# / WPF

收藏夹、置顶弹窗、剪贴板、HoneyView 都按 Windows 桌面能力做。未来若做「AI 抓 X」，可另写 Python 脚本写同一个 NocoDB。

不要选：Python + Tkinter、C# 控制台 + 网页前端。

---

## 开工前五项：已闭环

| # | 开工条件 | 当前状态 |
|---|---------|----------|
| 1 | 程序能稳定读写 NocoDB | **通过**。基座 `PM_Probe`，JWT + API Token |
| 2 | 重复任务多次完成能存、能查 | **通过**。`probe_tasks` + `probe_completions` Links |
| 3 | 提醒策略 | **通过（已改方案）**。不用 Toast；WPF 置顶弹窗人眼确认成功；托盘常驻 |
| 4 | 缩略图 + HoneyView + 剪贴板 | **通过**。剪贴板人眼确认；HoneyView 打开 200×200 多色图正常 |
| 5 | 探针用 C# | **通过** |

详细探针记录：`Personal_Management/Probes/last-probe-result.md`。

补测命令：

```bash
dotnet run --project Personal_Management/Probes/Probes.csproj -- --alert-window
dotnet run --project Personal_Management/Probes/Probes.csproj -- --honeyview
```

### 探针摘记（2026-08-22）

- NocoDB 2026.06.1，`http://localhost:8080`
- 上传附件 → 临时文件 → WPF 解码
- 剪贴板 SetImage / GetImage，人眼确认成功
- HoneyView：`D:\tools\Honeyview\Honeyview.exe`；200×200 四角红绿蓝黄 + 中心品红 + 渐变，显示正常
- Toast：放弃
- 置顶弹窗：成功

密钥和 HoneyView 路径在 gitignore 的 `nocodb-admin.txt`，不要写进本文件。

---

## 通了之后的业务纵切（原型可打乱顺序）

1. 任务列表：新建 + 列出（四种类型）
2. 标记完成 / 完成日期子表
3. 执行记录：点一下开始、再点结束
4. 奖励：L1–L3 抽奖次数（奖池可先写死）
5. 愿望单：额度增加 / 选择扣除
6. 每日任务巩固期 / 观察期（连续 7 天允许漏 1 天）
7. 到点 **置顶弹窗** 提醒
8. 周日程表
9. **收藏夹**（已做）：缩略图墙、tag / untagged、私密 tag 二级密码、HoneyView、剪贴板、批量导出。链接/视频可附缩略图。

**刻意后置：** AI 抓 X、Windows Toast。

---

## 原型默认（已开工）

「先随便做做 / 改，然后开工」之后按这些默认实现，不再逐条请示：

1. 关闭主窗口 = **藏到托盘**；托盘菜单「退出」才结束进程。
2. 正式数据基座：**`PersonalManagement`**（不用 `PM_Probe`）。
3. 第一刀：**任务 + 完成子表 + 执行记录 + 抽奖/愿望单占位 + 置顶提醒**。收藏夹第二刀。周日程表做一版简单的「本周」。
4. 界面：**中文**。
5. NocoDB 不通：ping `localhost:8080` → `docker start nocodb-vibecoding` → 必要时启动 Docker Desktop → 再 ping → 失败才报错。

代码在 `Personal_Management/Desktop`。

---

## 进入原型前想确认的问题

已按默认开工，下列仅作记录。

1. 关闭键 → 藏托盘（已按默认）。
2. 基座 `PersonalManagement`（已按默认）。
3. 第一刀不含收藏夹（已做第二刀）。
4. 中文（已按默认）。
5. **NocoDB 没开时：命令行拉起 Docker 容器**（已按讨论改，不再只弹「连不上」）。

# 个人管理工具迭代文档 11–13

> 本文件为用户体系与数据归属的设计草案。默认方案由实现侧补全；实现前以本文件为准。  
> **本迭代暂不写代码，仅定文档。**

---

## 迭代 11 — 用户体系与目录归属

### 目标

- 引入「用户」概念：无密码，仅用户名；本机多套数据隔离。
- 数据分两类：
  - **用户数据** → `projectRoot/UserData/<Username>/`
  - **程序数据** → `projectRoot/ProgramData/`

### 程序根目录 `projectRoot`（当前设定）

**定义：封包后（或日常运行）主程序** `.exe` **所在的文件夹。**

- 实现上通常取 `AppContext.BaseDirectory`（框架依赖发布时即 exe 旁目录）。
- **不再**用「向上查找 `个人管理工具需求.md` / `nocodb-admin.txt`」作为正式根（废弃现 `FindWorkspaceRoot()` 心智）。
- 布局为**便携式**：解压/拷贝整个程序目录即可带走；`UserData/`、`ProgramData/` 建在 exe 同级。
- **不要**默认安装到需管理员才能写的目录（如 `Program Files`）；分发形态按「解压即用」。
- **单文件发布**若把 BaseDirectory 指到临时解压目录，则不适合当数据根；本产品默认采用**框架依赖 / 标准文件夹发布**（exe 与依赖同目录长期存在）。
- **开发期：** `dotnet run` 时 BaseDirectory 常在 `bin/Debug|Release/...`，数据会落在输出目录旁。可接受；或实现时可选环境变量 / 开关把数据根指到固定开发目录（非必须，实现阶段再定）。勿把开发输出目录里的 UserData 提交进 git。

对比旧行为（仅作迁移理解）：


|       | 旧（当前代码）                                    | 新（本文件设定）                                 |
| ----- | ------------------------------------------ | ---------------------------------------- |
| 根     | 含需求文档或 admin txt 的「工作区」（如仓库 `VibeCoding/`） | **exe 所在目录**                             |
| 数据常落在 | `{工作区}/Personal_Management/...`            | `{exe目录}/UserData`、`{exe目录}/ProgramData` |




### 关于 `nocodb-admin.txt`（现状评估）

**程序现在依赖它吗？**


| 用途                     | 是否硬依赖      | 说明                                                                        |
| ---------------------- | ---------- | ------------------------------------------------------------------------- |
| 定位工作区根（旧逻辑）            | **否（二选一）** | 旧代码：有 `个人管理工具需求.md` / `需求.md` / `nocodb-admin.txt` **任一**即可；新设定**不再用它找根** |
| Noco 连接凭据              | **软依赖（旧）** | 旧：`storage.json` 未填回退该文件；新：只读用户 `settings.json`                           |
| HoneyView / Docker 容器名 | **软依赖（旧）** | 同上                                                                        |
| 运行「必须有这个文件」            | **否**      | 全本地即可跑；txt 不是正式配置                                                         |


**结论（默认方案）：**

- **找根只认 exe 目录**；与 txt、需求文档脱钩。
- **Noco / HoneyView / Container / LLM 一律 per-user**，写入该用户 `settings.json`；**运行时不再回退读 txt**。
- 迁移时：若在旧工作区根或 `Personal_Management` 旁发现 `nocodb-admin.txt`，将其内容**合并进迁移目标用户**的设置（仅填空字段）。
- 仓库继续 gitignore 该文件；注明「仅历史/本机草稿，非程序正式配置」。



### 归属表

路径均相对 `**projectRoot`（exe 所在目录）**。`<User>` = 规范化后的用户名目录。

#### A. 程序数据 `ProgramData/`（全局，与当前登录用户无关）


| 项                    | 建议路径                                     | 说明                                    |
| -------------------- | ---------------------------------------- | ------------------------------------- |
| 程序配置                 | `ProgramData/app.json`                   | 含：是否「直接登录」、上次登录用户名、用户列表索引（可选）、数据布局版本号 |
| 内置主题定义               | （代码内 / 可选 `ProgramData/builtin-themes/`） | 内置「普通/黑夜」不随用户；**用户主题副本**见用户侧          |
| 迁移记录                 | `ProgramData/migration.json`             | 是否已从旧布局迁移、来源路径等                       |
| 旧 `nocodb-admin.txt` | 不在 ProgramData                           | 仅可能出现在旧仓库根；迁移时读一次，非正式运行时配置            |




#### B. 用户数据 `UserData/<User>/`（per-user）


| 项                 | 建议路径                                       | 说明                                                                                                                        |
| ----------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- |
| 用户设置（含 Noco）      | `UserData/<User>/settings.json`            | 承接现 `storage.json`：**三库 Noco 开关、URL/Email/Password/ApiToken、ConnectMode、Container、HoneyView、LLM***；**Noco 连接全部 per-user** |
| 本地业务库             | `UserData/<User>/local/business/`          | 现 `Personal_Management/local/business/`（JSON 表 + `files/`）                                                                |
| 本地收藏库             | `UserData/<User>/local/favorites/`         | 同上                                                                                                                        |
| 本地体重库             | `UserData/<User>/local/weight/`            | 同上                                                                                                                        |
| 界面主题（含用户副本）       | `UserData/<User>/styles.json`              | 现 `Personal_Management/styles.json`                                                                                       |
| 窗口几何 / 栏列数 / 提示抑制 | `UserData/<User>/window.json`              | 现 `window.json`（按用户记窗口更合理）                                                                                                |
| 日程块样式预设           | `UserData/<User>/block-style-presets.json` | 现同名文件                                                                                                                     |
| 自定义开始任务图标等        | `UserData/<User>/assets/`                  | 现 `Desktop/Assets/user/` 一类用户资源                                                                                           |
| 用户元信息（可选）         | `UserData/<User>/profile.json`             | 显示名、创建时间等；用户名以目录名为准                                                                                                       |




#### C. 明确不进用户目录 / 不进 ProgramData 的


| 项                        | 归属                   | 说明                            |
| ------------------------ | -------------------- | ----------------------------- |
| 源码、`bin/`/`obj/`、仓库文档    | 工程仓库                 | 与封包根分离；需求/迭代文档**不再参与找根**      |
| exe 与依赖 dll 等            | `projectRoot` 内由发布产出 | 非「用户数据」                       |
| `nocodb-data/`（Docker 卷） | 本机 Docker            | 不进 UserData；靠 per-user 连接配置区分 |
| HoneyView **程序本体**       | 本机安装路径               | 只存路径字符串在用户 `settings.json`    |




#### D. 现路径 → 新路径（迁移对照）

旧数据相对**旧工作区**（含需求文档的目录）下的 `Personal_Management/`：


| 现路径（相对旧工作区）                                    | 新归属（相对 exe 目录）                             |
| ---------------------------------------------- | ------------------------------------------ |
| `Personal_Management/local/**`                 | `UserData/<迁移用户>/local/**`                 |
| `Personal_Management/storage.json`             | `UserData/<迁移用户>/settings.json`            |
| `Personal_Management/styles.json`              | `UserData/<迁移用户>/styles.json`              |
| `Personal_Management/window.json`              | `UserData/<迁移用户>/window.json`              |
| `Personal_Management/block-style-presets.json` | `UserData/<迁移用户>/block-style-presets.json` |
| `Personal_Management/Desktop/Assets/user/**`   | `UserData/<迁移用户>/assets/**`                |
| 旧工作区根 `nocodb-admin.txt`                       | 合并进该用户 `settings.json` 空字段                 |
| （无）                                            | 新建 `ProgramData/app.json`                  |


迁移完成后，旧 `Personal_Management/local` 与旧 json：**默认保留但不再读写**（可在后续迭代提供「清理旧数据」）；避免首版误删。

---



## 迭代 12 — 登录、新建用户、直接登录



### 启动流程（默认）

1. `projectRoot` = exe 所在目录；确保存在 `ProgramData/`（可空建）。
2. 若检测到旧布局且尚无任何 `UserData/<User>/` → 走**一次性迁移**（见迭代 13），再进入登录逻辑。
3. 读 `ProgramData/app.json`：
  - **直接登录 = 开** 且上次用户目录仍存在 → 跳过登录页，以该用户加载设置与 local，进主窗口。
  - 否则 → 显示登录页。



### 登录页


| 场景         | UI                                                  |
| ---------- | --------------------------------------------------- |
| 没有任何用户     | 仅「新增用户」（用户名 + 三库 Noco 勾选，默认全不勾；至少勾一项才可编辑 Noco 连接字段） |
| 已有用户且未直接登录 | **默认方案：** 用户列表（点选进入）+「新增用户」；无密码。暂不提供删除用户 UI（可后续加）   |
| 直接登录开启     | 不停留登录页（见上）                                          |




### 新建用户（默认规则）

- 用户名：Trim 后非空；禁止 `\/:*?"<>|` 与全 `.`；长度建议 1–64；**目录名按原文创建**（Windows 大小写不敏感，创建前与已有用户做忽略大小写查重）。
- Noco 三开关默认 **全关**；全关时连接字段只读/隐藏。
- 创建目录骨架：`settings.json`（默认值）、空 `local/{business,favorites,weight}/`（启动时按现逻辑 EnsureSeeded）。
- 创建成功后：写入「上次用户」= 该用户，并进入主窗口（等同登录）。
- **暂不提供登出**；换用户：关「直接登录」后重启选人，或退出进程再开。



### 直接登录

- 开关存在 `**ProgramData/app.json**`（程序级，非 per-user）。
- 默认：**开启**。
- 设置页（建议放在「设置」或「界面」旁的程序级区）可关闭。
- 关闭后下次启动必经登录页（有用户列表）。
- 若上次用户目录丢失：清掉 last-user，进入登录页（有用户则列表，无则新增）。



### 设置页与 Noco（默认）

- 现有 Noco/LLM/HoneyView 编辑仍在设置页，读写**当前用户** `settings.json`。
- 登录页新建时的勾选与字段，与设置页同一套模型。
- **不再显示「未填回退 nocodb-admin.txt」**；缺项即未配置。

---



## 迭代 13 — 迁移与兼容



### 触发

首次启动同时满足：

- 能定位到旧数据痕迹（见下）；
- `projectRoot/UserData/` 下尚无任何用户目录。



### 如何找到旧数据（相对新根）

因新根是 exe 目录，旧数据可能仍在仓库布局中。默认探测顺序（实现可微调）：

1. `projectRoot/Personal_Management/`（若有人把发布目录与旧结构放一起）；
2. 从 `projectRoot` **向上**有限层查找含 `Personal_Management/storage.json` 或 `Personal_Management/local` 的目录（兼容「从仓库 bin 跑、数据在仓库 `Personal_Management`」的开发迁移）；
3. 同层发现的旧工作区根上的 `nocodb-admin.txt` 仅用于合并凭据。

若找不到旧数据 → 不迁移，直接空用户登录流。

### 迁移用户名（默认）

- 登录页（或迁移专用一步）要求输入用户名，文案说明「将把当前本机数据归入该用户」。
- 不自动用 `default`，避免用户不知情。



### 迁移步骤（默认）

1. 创建 `UserData/<User>/` 并按归属表拷贝文件与 `local/**`。
2. 将 `storage.json` → `settings.json`；缺字段用找到的 `nocodb-admin.txt` 补齐。
3. 写 `ProgramData/app.json`：`directLogin=true`，`lastUser=<User>`，版本号。
4. 不删除旧文件；写 `ProgramData/migration.json` 记录已迁移与来源路径，防止重复迁移。



### .gitignore / 备份（文档约定，改 ignore 属实现）

后续实现时应忽略例如：

- `UserData/`、`ProgramData/`（含可能出现在 `bin/**` 下的）
- 继续忽略旧路径（若仍残留）与 `nocodb-admin.txt`

完整备份（封包目录）= 整个 exe 目录中的 `UserData/` + `ProgramData/`（+ 若用 Docker 则另备 `nocodb-data/`）。

---



## 本文件未覆盖、留待以后的

- 删除用户、重命名用户、导出/导入单用户包  
- 登出按钮、登录页 PIN  
- 多用户共用同一 Noco 时的库表隔离策略（当前假定连接配置不同或人工分库）  
- 自动清理旧 `Personal_Management/local`  
- 安装到 Program Files 时改写 `%AppData%` 的变体

---



## 验收要点（实现后）

- [ ] `projectRoot` 为 exe 所在目录；其下出现 `UserData` / `ProgramData`，不依赖需求文档找根  
- [ ] 无用户时只能新增；全本地默认可跑，不依赖 txt  
- [ ] Noco 配置只存在用户 `settings.json`，per-user  
- [ ] 旧 `Personal_Management` 数据可一次迁入指定用户并直接可用  
- [ ] 直接登录可关；关掉后能选用户  
- [ ] 归属表所列文件均落在约定目录  